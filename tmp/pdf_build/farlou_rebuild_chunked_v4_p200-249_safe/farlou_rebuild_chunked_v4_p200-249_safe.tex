% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  ukrainian,
]{article}
\usepackage{xcolor}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
\setmainfont{DejaVu Serif}
\setsansfont{DejaVu Sans}
\setmonofont{DejaVu Sans Mono}
\setmathfont{STIX Two Math}
\fi
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{graphicx}
\graphicspath{{/Users/kalinicenkovitalijmikolajovic/.codex/worktrees/5c15/Script_for_translate_book/Output_Final/}}
\setkeys{Gin}{width=0.82\linewidth,height=0.82\textheight,keepaspectratio}
\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{% scales image to fit in text height/width
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi% select the smaller of both
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi%
}
% Set default figure placement to htbp
\def\fps@figure{htbp}
\makeatother
\ifLuaTeX
\usepackage[bidi=basic,shorthands=off,provide=*]{babel}
\else
\usepackage[bidi=default,shorthands=off,provide=*]{babel}
\fi
\ifLuaTeX
  \usepackage{selnolig} % disable illegal ligatures
\fi
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  pdflang={uk},
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\author{}
\date{}

\begin{document}

\pandocbounded{\includegraphics[keepaspectratio]{images/_page_205_Figure_5.jpeg}}

Рис. 28 1. Типичная зависимость потока от плотности.Теперь мы
подготовлены к тому, чтобы применить уравнение \texttt{u\_i+f\_x=0} к
задаче дорожного движения. По правилу дифференцирования сложной функции
\texttt{f\_x=\textbackslash{}frac\{df\}\{du\}\textbackslash{},u\_x} ;
значит, закон сохранения можно переписать в виде

\begin{verbatim}
u_t + \frac{df}{du} u_x = u_t + g(u) u_x = 0.
\end{verbatim}

Пусть, например, зависимость потока от плотности квадратичная, т. е.
\texttt{f(u)\ =\ u\^{}\{2\}} , тогда закон сохранения принимает
следующую форму:

\begin{verbatim}
u_t + 2uu_x = 0.
\end{verbatim}

Если начальная плотность распределения автомобилей была
\texttt{u(x,\ 0)\ =\ \textbackslash{}varphi(x)} , то, чтобы найти
плотность распределения в произвольный момент времени, необходимо решить
задачу с начальным условием (задачу Коши):

\begin{verbatim}
\begin{array}{llll} (\text{УЧП}) & u_t + 2uu_x = 0, & -\infty < x < \infty, & 0 < t < \infty, \\ (\text{HV}) & u\left(x, \ 0\right) = \varphi\left(x\right), & -\infty < x < \infty. \end{array}
\end{verbatim}

Займемся теперь решением нелинейной задачи Коши.\# Нелинейная задача с
начальным условием (задача Коши)Рассмотрим задачу

\begin{verbatim}
\begin{array}{llll} (\mathsf{Y}\mathsf{P}\Pi) & u_t + g\left(u\right)u_x = 0, & -\infty < x < \infty, & 0 < t < \infty, \\ (\mathsf{H}\mathsf{Y}) & u\left(x, \ 0\right) = \varphi\left(x\right), & -\infty < x < \infty. \end{array}
\end{verbatim}

Вспомним предыдущую лекцию, где мы изучали уравнение конвективного
переноса:

\begin{verbatim}
u_t + vu_x = 0
\end{verbatim}

.У цьому рівнянні \texttt{u(x,\ t)} --- це концентрація матерії в
потоку, що рухається зі швидкістю v

\begin{verbatim}
u_1 + g(u)u_x = 0
\end{verbatim}

и уравнением переноса. Будем считать, что частина воды в гочке
\texttt{x\_0} движется со скоростью \texttt{g(u)} (вдоль по течению или
против него). Тогда через t секунд координата частины будет

\begin{verbatim}
x = x_0 + g(u) t
\end{verbatim}

(уравнение характеристик).Пам'ятайте, що концентрація
\texttt{u\ (x,\ t)} не змінюється вздовж характеристики. Якщо ми знаємо
початкову концентрацію \texttt{u(x\_a,\ 0)} ,

\pandocbounded{\includegraphics[keepaspectratio]{images/_page_206_Figure_8.jpeg}}

Рис. 28.2. Характеристики уравнения \texttt{u\_t+g} (u) \texttt{u\_x=0}
; a---общий вид характеристики, выходящей из т. ( \texttt{x\_0} , 0),
вдоль характеристики решение остается постоянным и равным величине
\texttt{u\ (} x\_0 \texttt{,\ 0)}; b---пример характеристики,
начинающейся из т. (2, 0), уравнения \texttt{u\_t+3uu\_x=0} с
\texttt{u\ (2,\ 0)}=1.то уравнение характеристик принимает вид
\texttt{x\ =\ x\_0\ +\ g{[}u(x\_0,\ 0){]}\ t} (уравнение характеристики,
начинающейся в точке \texttt{(x\_0,\ 0)} ).Пусть, например, мы решаем
задачу(УЧП)

\begin{verbatim}
u_t + 3uu_x = 0
\end{verbatim}

,\\
(НУ) u(x, 0) =

\begin{verbatim}
u_t = Du_{xx} - Vu_x.
\end{verbatim}

Чтобы найти характеристику, выходящую из точки, например (2, 0), мы
должны написать

\begin{verbatim}
x = 2 + g[u(2, 0)]t =
\end{verbatim}

\texttt{=\ \textquotesingle{}2\ +\ g{[}1{]}t\ =\textquotesingle{}\ =\ \textquotesingle{}2\ +\ 3t\textquotesingle{}\ .Можна\ стверджувати,\ що\ розв\textquotesingle{}язок\ нашої\ задачі\ \textquotesingle{}u(x,\ t)\textquotesingle{}\ дорівнює\ одному\ на\ лінін\ x=2+3t,\ тобто\ \textquotesingle{}u(2+3t,\ t)\textquotesingle{}=1\ для\ всіх\ t\textgreater{}0.\ Фіг.\ 28.2\ показує\ характеристики\ загального\ випадку\ та\ цього\ конкретного\ завдання.Тепер\ зрозуміло,\ що,\ знаючи\ характеристики\ в\ кожній\ точці\ і\ знаючи,\ що\ розв\textquotesingle{}язок\ за\ характеристиками\ не\ змінюється,\ можна\ отримати\ розв\textquotesingle{}язок\ \textquotesingle{}u\textbackslash{}left(x,\textbackslash{}\ t\textbackslash{}right)\textquotesingle{}\ у\ будь-який\ момент\ часу\ t.\ Ми\ не\ отримали\ явного\ виразу\ для\ \textquotesingle{}u\textbackslash{}left(x,\textbackslash{}\ t\textbackslash{}right)\textquotesingle{}\ через\ змінні\ x\ і\ t,\ але\ знання\ характеристик\ рівняння\ дозволяє\ розв\textquotesingle{}язати\ деякі\ цікаві\ задачі.\#\ Задача\ дорожного\ движенияЗаймемся\ моделью\ дорожного\ движения,\ в\ которой\ поток\ вычисляется\ по\ формуле\ \textquotesingle{}f(u)=u\^{}2\textquotesingle{}\ ,\ а\ начальная\ плотность\ изо-\ Р\ И\ С}

\pandocbounded{\includegraphics[keepaspectratio]{images/_page_207_Figure_8.jpeg}}

. 28.3. Начальная плотность автомобилей движущихся слева направо.бражена
на рис. 28.3. Другими словами, нам нужно решить задачу

\begin{verbatim}
(\text{YUII}) \quad u_t + 2uu_x = 0, \quad -\infty < x < \infty, \quad 0 < t < \infty,
(\text{HV}) \quad u(x, \ 0) =
\begin{cases} 1, & x \leq 0, \\ 1 - x, & 0 < x < 1, \\ 0, & 1 \leq x. \end{cases}
\end{verbatim}

Начнем с определения характеристик, выходящих из точек вида
\texttt{(x\_0,\ 0)} . Для \texttt{x\_0\ \textless{}\ 0} получаем

\begin{verbatim}
x = x_0 + g [u(x_0, 0)] t =
\end{verbatim}

= \texttt{x\_0\ +\ g\ {[}1{]}\ t\ =} = \texttt{x\_0\ +\ 2t} .Разрешая
относительно t, находим уравнение характеристик

\begin{verbatim}
t = \frac{1}{2} (x - x_0).
\end{verbatim}

Эти прямые линии изображены на рис. 28.4. Теперь рассмотрим точки
\texttt{0\ \textless{}\ x\_0\ \textless{}\ 1} . Здесь характеристики
определяются следующими соотношениями:

\begin{verbatim}
x = x_0 + g[u(x_0, 0)] t =
= x_0 + g[(1 - x_0)] t =
= x_0 + 2(1 - x_0) t.
\end{verbatim}

Разремая последнее соотношение относительно t, получаем

\begin{verbatim}
t = \frac{x - x_0}{2(1 - x_0)}.
\end{verbatim}

При \texttt{1\ ≤\ x\_0\ \textless{}\ ∞} характеристики

\begin{verbatim}
x = x_0 + g[u(x_0, 0)]t =
\end{verbatim}

= \texttt{x\_0\ +\ g{[}0{]}t\ =} = \texttt{x\_0} приводят к вертикальным
линиям, выходящим из точек хо. Все характеристики задачи изображены на
рис. 28.4.

\pandocbounded{\includegraphics[keepaspectratio]{images/_page_208_Figure_10.jpeg}}

Рис. 28.4. Характеристики уравнения \texttt{u\_t\ +\ 2uu\_x\ =\ 0}
.Ситуація на дорозі при 0 \textless{} t \textless{} 1/2 досить
зрозуміла. Рис. 28.5 показує розв'язок нашої задачі в різні моменти
часу. Зверніть увагу, що характеристики сходяться в одній точці t=1/2.
Отже, щоб розв'язати задачу на t\textgreater1/2, потрібно
використовувати інший метод. Коли характеристики сходяться в одній
точці, говоримо про ударну хвилю (розрив розв'язку). Тепер потрібно
відповісти на питання: на якій швидкості є передній крайударной волны
будет перемещаться вдоль автострады? Хотя это и не очевидно, но
оказывается, что скорость распространения разрыва находится по формуле

\begin{verbatim}
S = \frac{f(u_R) - f(u_L)}{u_R - u_L},
\end{verbatim}

где \texttt{u\_R} и \texttt{u\_L} ---значения решения справа и слева от
волнового фронта, а \texttt{f(u\_R)} и \texttt{f(u\_L)} ---значения
потока при этих плотностях.

\pandocbounded{\includegraphics[keepaspectratio]{images/_page_209_Figure_5.jpeg}}

Рис. 28.5. Плотность дорожного движения в различные моменты времени.У
нашому прикладі швидкість поширення щілини виявляється рівною одиниці.
Насправді, якщо згадати цей \texttt{f(u)\ =\ u\^{}2} , то одразу
отримуємо

\begin{verbatim}
S = \frac{0-1}{0-1} = 1
\end{verbatim}

.Это означает, что при t\textgreater1/2 фронт волны будет перемещаться
слева направо со скоростью единица.Полное решение задачи

\begin{verbatim}
(\text{Y} \, \Pi) \quad u_t + 2uu_x = 0, \quad -\infty < x < \infty,
(\text{HY}) \quad u(x, \ 0) =
\begin{cases} 1, & x \leq 0, \\ 1 - x, & 0 < x < 1, \\ 0, & 1 < x, \end{cases}
\end{verbatim}

изображено на рис. 28.5. \# ЗАУВАЖЕННЯ 1. Ударна хвиля в нашому прикладі
виникає через те, що потік швидко зростає зі збільшенням густини u. Якщо
потік заданий формулою \texttt{f(u)} = u, то рівняння було б у вигляді
\texttt{u\_1+u\_x=0} , і розв'язок цього рівняння представляє хвилю, що
рухається вправо без спотворень. Якщо на мить замислитися над значенням
формули \texttt{f(u)} = u, стає очевидно, що розв'язок рухатиметься
таким чином.2. Прямой подстановкой решения в виде

\begin{verbatim}
u = \varphi(x - g(u) t)
\end{verbatim}

можно убедиться в том, что эта формула неявно определяет решение задачи

\begin{verbatim}
\begin{array}{lll} u_t + g\left(u\right) u_x = 0, & -\infty < x < \infty, & 0 < t < \infty, \\ u\left(x, \ 0\right) = \varphi\left(x\right), & -\infty < x < \infty. \end{array}
\end{verbatim}

Например, решение задачи Коши

\begin{verbatim}
(Y\Pi\Pi) \quad u_t + uu_x = 0,
(HY) \quad u(x, 0) = x
\end{verbatim}

неявно вадается соотношением

\begin{verbatim}
u = \varphi [x - g(u) t] =
= x - g(u) t = x - ut.
\end{verbatim}

У нашому конкретному випадку можливо отримати рішення у явній формі (у
багатьох інших випадках це неможливо)

\begin{verbatim}
u(x, t) = \frac{x}{1+t}.
\end{verbatim}

Проверьте, что эта функция действительно является решением нашей
задачи.\# \textbf{ЗАДАЧИ}1. Решите задачу Коши

\begin{verbatim}
\begin{array}{llll} (\text{YUII}) & u_t + uu_x = 0, & -\infty < x < \infty, & 0 < t < \infty, \\ (\text{HV}) & u(x, \ 0) = \begin{cases} 0, & x < 0, \\ x, & 0 \leqslant x. \end{cases} \end{array}
\end{verbatim}

Постройте график решения для различных моментов времени. Как вы
интерпретируете это решение? Как выглядит соотношение между потоком и
плотностью в этой задаче?2. Решите задачу

\begin{verbatim}
\begin{split} & (\text{УЧП}) \quad u_t + u^2 u_x = 0, \quad -\infty < x < \infty, \quad 0 < t < \infty, \\ & (\text{НУ}) \quad u_t(x, 0) = \begin{cases} 0, & x < 0, \\ x, & 0 \leqslant x. \end{cases} \end{split}
\end{verbatim}

Какова здесь связь между потоком и плотностью? Совнадает ли поведение
полученного решения с тем, что вы ожидали? Сравните решения задачи 1 и
задачи 2.3. Предположим, что невязкая жидкость течет по трубе и при этом
просачивается через стенки по закону \texttt{F(u)} = ku (г/см·с). Закон
сохранения (скорее закон изменения) в этом случае записывается в виде

\begin{verbatim}
u_t + f_x = -F(u)
\end{verbatim}

.Знайдіть розв'язок цього рівняння, якщо \texttt{f(u)} = u і
\texttt{\textbackslash{}varphi(x)\ =\ u} (x, 0) (початковий розподіл
задається загальним чином). Дайте тлумачення отриманого рішення.4.
Знайти розв'язок попереднього рівняння, якщо втрати визначаються
функцією \texttt{F(x,\ t)} = 1/x. Перевірте розв'язок. Ви розумієте його
фізичне значення?5. Проверьте, что функция и, неявно заданная
соотношением

\begin{verbatim}
u = \varphi[x - g(u) t],
\end{verbatim}

является решением нелинейной задачи

\begin{verbatim}
(\text{УЧП}) \quad \begin{array}{ll} u_1 + g(u) u_x = 0, \\ (\text{НУ}) & u(x, 0) = \varphi(x). \end{array}
\end{verbatim}

\section{\texorpdfstring{СИСТЕМЫ УРАВНЕНИЙ С ЧАСТНЫМИ ПРОИЗВОДНЫМИ МЕТА
ЛЕКЦІЇ: Познакомить читателя с тем фактом, что многие физические системы
(особенно в динамике жидкости) нельзя описать одним уравнением. Для
описания таких систем требуются системы связанных уравнений. В эти
системы входят некоторые неизвестные функции, такие, как давление
\texttt{p(x,\ y,\ z,\ t)}, плотность
\texttt{\textbackslash{}rho(x,\ y,\ z,\ t)} , температура
\texttt{T(x,\ y,\ z,\ t)} и их частные производные. Связи между этими
функциями определяются физическими законами и наша задача --- найти все
эти функции одновременно.В лекции будет показано, как можно решить
линейную систему
уравнений}{СИСТЕМЫ УРАВНЕНИЙ С ЧАСТНЫМИ ПРОИЗВОДНЫМИ МЕТА ЛЕКЦІЇ: Познакомить читателя с тем фактом, что многие физические системы (особенно в динамике жидкости) нельзя описать одним уравнением. Для описания таких систем требуются системы связанных уравнений. В эти системы входят некоторые неизвестные функции, такие, как давление p(x, y, z, t), плотность \textbackslash rho(x, y, z, t) , температура T(x, y, z, t) и их частные производные. Связи между этими функциями определяются физическими законами и наша задача --- найти все эти функции одновременно.В лекции будет показано, как можно решить линейную систему уравнений}}\label{ux441ux438ux441ux442ux435ux43cux44b-ux443ux440ux430ux432ux43dux435ux43dux438ux439-ux441-ux447ux430ux441ux442ux43dux44bux43cux438-ux43fux440ux43eux438ux437ux432ux43eux434ux43dux44bux43cux438-ux43cux435ux442ux430-ux43bux435ux43aux446ux456ux457-ux43fux43eux437ux43dux430ux43aux43eux43cux438ux442ux44c-ux447ux438ux442ux430ux442ux435ux43bux44f-ux441-ux442ux435ux43c-ux444ux430ux43aux442ux43eux43c-ux447ux442ux43e-ux43cux43dux43eux433ux438ux435-ux444ux438ux437ux438ux447ux435ux441ux43aux438ux435-ux441ux438ux441ux442ux435ux43cux44b-ux43eux441ux43eux431ux435ux43dux43dux43e-ux432-ux434ux438ux43dux430ux43cux438ux43aux435-ux436ux438ux434ux43aux43eux441ux442ux438-ux43dux435ux43bux44cux437ux44f-ux43eux43fux438ux441ux430ux442ux44c-ux43eux434ux43dux438ux43c-ux443ux440ux430ux432ux43dux435ux43dux438ux435ux43c.-ux434ux43bux44f-ux43eux43fux438ux441ux430ux43dux438ux44f-ux442ux430ux43aux438ux445-ux441ux438ux441ux442ux435ux43c-ux442ux440ux435ux431ux443ux44eux442ux441ux44f-ux441ux438ux441ux442ux435ux43cux44b-ux441ux432ux44fux437ux430ux43dux43dux44bux445-ux443ux440ux430ux432ux43dux435ux43dux438ux439.-ux432-ux44dux442ux438-ux441ux438ux441ux442ux435ux43cux44b-ux432ux445ux43eux434ux44fux442-ux43dux435ux43aux43eux442ux43eux440ux44bux435-ux43dux435ux438ux437ux432ux435ux441ux442ux43dux44bux435-ux444ux443ux43dux43aux446ux438ux438-ux442ux430ux43aux438ux435-ux43aux430ux43a-ux434ux430ux432ux43bux435ux43dux438ux435-px-y-z-t-ux43fux43bux43eux442ux43dux43eux441ux442ux44c-rhox-y-z-t-ux442ux435ux43cux43fux435ux440ux430ux442ux443ux440ux430-tx-y-z-t-ux438-ux438ux445-ux447ux430ux441ux442ux43dux44bux435-ux43fux440ux43eux438ux437ux432ux43eux434ux43dux44bux435.-ux441ux432ux44fux437ux438-ux43cux435ux436ux434ux443-ux44dux442ux438ux43cux438-ux444ux443ux43dux43aux446ux438ux44fux43cux438-ux43eux43fux440ux435ux434ux435ux43bux44fux44eux442ux441ux44f-ux444ux438ux437ux438ux447ux435ux441ux43aux438ux43cux438-ux437ux430ux43aux43eux43dux430ux43cux438-ux438-ux43dux430ux448ux430-ux437ux430ux434ux430ux447ux430-ux43dux430ux439ux442ux438-ux432ux441ux435-ux44dux442ux438-ux444ux443ux43dux43aux446ux438ux438-ux43eux434ux43dux43eux432ux440ux435ux43cux435ux43dux43dux43e.ux432-ux43bux435ux43aux446ux438ux438-ux431ux443ux434ux435ux442-ux43fux43eux43aux430ux437ux430ux43dux43e-ux43aux430ux43a-ux43cux43eux436ux43dux43e-ux440ux435ux448ux438ux442ux44c-ux43bux438ux43dux435ux439ux43dux443ux44e-ux441ux438ux441ux442ux435ux43cux443-ux443ux440ux430ux432ux43dux435ux43dux438ux439}

\begin{verbatim}
u_t + Au_x = 0
\end{verbatim}

если преобразовать ее в новую систему независимых уравнений

\begin{verbatim}
v_1 + \Lambda v_x = 0,
\end{verbatim}

а затем независимо решить каждое уравнение этой системы.У багатьох
галузях науки існують системи величин, які взаємопов'язані не одним, а
кількома відношеннями одночасно. Наприклад, у гідродинамікі це чотири
рівняння

\begin{verbatim}
u_t + uu_x + vu_y + \frac{1}{\rho} p_x = 0
\end{verbatim}

\texttt{(сохранение\ импульса\ вдоль\ оси\ \textquotesingle{}x\textquotesingle{}\ ),\ (29.1)\ \textquotesingle{}v\_t\ +\ uv\_x\ +\ vv\_y\ +\ \textbackslash{}frac\{1\}\{\textbackslash{}rho\}\ p\_y\ =\ 0\textquotesingle{}\ (сохранение\ импульса\ вдоль\ оси\ \textquotesingle{}y\textquotesingle{}\ ),\ (сохранение\ массы),\ (сохранение\ массы),\ (сохранение\ массы),\ \textquotesingle{}\textbackslash{}left(\textbackslash{}frac\{p\}\{\textbackslash{}rho\^{}\{\textbackslash{}gamma\}\}\textbackslash{}right)\_t\ +\ u\textbackslash{}left(\textbackslash{}frac\{p\}\{\textbackslash{}rho\^{}\{\textbackslash{}gamma\}\}\textbackslash{}right)\_x\ +\ v\textbackslash{}left(\textbackslash{}frac\{p\}\{\textbackslash{}rho\^{}\{\textbackslash{}gamma\}\}\textbackslash{}right)\_y\ =\ 0\textquotesingle{}\ (сохранение\ энергии),\ \textquotesingle{}p(x,\ y,\ z,\ t)\textquotesingle{}\ —\ давление\ в\ жидкости,\ \textquotesingle{}u(x,\ y,\ z,\ t)\textquotesingle{}\ —\ \textquotesingle{}x\textquotesingle{}\ -компонента\ скорости,\ \textquotesingle{}v(x,\ y,\ z,\ t)\textquotesingle{}\ —\ \textquotesingle{}y\textquotesingle{}\ -компонента\ скорости,\ \textquotesingle{}\textbackslash{}rho(x,\ y,\ z,\ t)\textquotesingle{}\ —\ плотность\ жидкости.Приведенная\ нелинейная\ система\ уравнений\ называется\ уравнениями\ Эйлера\ движения\ жидкости.\ Задача\ состоит\ в\ одновременном\ отыскании\ всех\ неизвестных\ функций\ р,\ и,\ v\ и\ р,\ которые\ удовлетворяли\ бы\ всем\ четырем\ уравнениям\ (вместе\ с\ начальными\ и\ граничными\ условиями).Існують\ й\ інші\ причини\ вивчати\ системи\ рівнянь.\ У\ теорії\ звичайних\ диференціальних\ рівнянь\ (якщо\ читач\ пам\textquotesingle{}ятає)\ показано,\ що\ одне\ рівняння\ другого\ порядку\ можна\ представити\ як\ систему\ з\ двох\ рівнянь\ першого\ порядку.\ Хоча\ теорія\ диференціальних\ рівнянь\ у\ похідних\ не\ така\ проста,\ дуже\ часто\ одне\ рівняння\ з\ похідними\ у\ похідних\ порядків\ зводиться\ до\ системи\ диференціальних\ рівнянь\ першого\ порядку.\ Наприклад,\ телеграфне\ рівняння}

\begin{verbatim}
u_{tt} = c^2 u_{xx} + a u_t + b u
\end{verbatim}

путем введения трех новых переменных

\begin{verbatim}
u_1 = u_1
u_2 = u_2
u_3 = u_4
\end{verbatim}

можно свести к системе уравнений первого порядка(29.2)

\begin{verbatim}
\begin{cases} \frac{\partial u_1}{\partial x} = u_2, \\ \frac{\partial a_1}{\partial t} = u_3, \\ \frac{\partial u_3}{\partial t} = c^2 \frac{\partial u_2}{\partial x} + au_3 + bu_4. \end{cases}
\end{verbatim}

Для того чтобы решать системы типа (29.2), необходимо хорошо знать такие
понятия, как собственные значения и собственные векторы матрицы. По эфой
причине мы предлагаем вниманию читателя краткий обзор линейной
алгебры.\# Обзор линейной алгебрыОбщий случайЧастный случайОпределение.
Если А---квадратная матрица порядка n, то собственными значениями
матрицы называются n корней характе-Пусть

\begin{verbatim}
A =
\begin{bmatrix} 1 & 1 \\ 4 & 1 \end{bmatrix}
\end{verbatim}

.ристического уравнения

\begin{verbatim}
\det\left(A-\lambda I\right)=0,
\end{verbatim}

\begin{verbatim}
где  \det(A-\lambda I)  — определитель матрицы  A-\lambda I  .Деякі власні значення можуть збігатися.Тогда собственные значения являются корнями уравнения
\end{verbatim}

\begin{verbatim}
\det
\begin{bmatrix} 1-\lambda & 1\\ 4 & 1-\lambda \end{bmatrix}
= (1-\lambda)^2 - 4 =
= \lambda^2 - 2\lambda - 3 = 0.
\end{verbatim}

Откуда

\begin{verbatim}
\lambda_1 = -1
\end{verbatim}

, \texttt{\textbackslash{}lambda\_2\ =\ 3} .Визначення. Якщо
\texttt{\textbackslash{}lambda} --- власне значення матриці A, то
відповідний власний вектор є таким ненульовим вектором, що задовольняє
відношення

\begin{verbatim}
Ax = \lambda x
\end{verbatim}

.Пример. Вектор X =

\begin{verbatim}
u_t = -Vu_x
\end{verbatim}

\texttt{является\ собственным\ вектором,\ соответствующим\ собственному\ значению}
\lambda\_2 = 3 , так как

\begin{verbatim}
\begin{bmatrix} 1 & 1 \\ 4 & 1 \end{bmatrix}
\begin{bmatrix} 1 \\ 2 \end{bmatrix}
= 3
\begin{bmatrix} 1 \\ 2 \end{bmatrix}
\end{verbatim}

.

Определение. Матрица \texttt{A\^{}\{-1\}} называется обратной к матрице
A, если

\begin{verbatim}
AA^{-1} = A^{-1}A = I_1
\end{verbatim}

Пример, Обратной к

\begin{verbatim}
A =
\begin{bmatrix} 2 & -2 \\ 1 & 1 \end{bmatrix}
\end{verbatim}

является матрица

\begin{verbatim}
A^{-1} =
\begin{bmatrix} 0.25 & 0.5 \\ -0.25 & 0.5 \end{bmatrix}
\end{verbatim}

,

де I - одинична матриця.поскольку

\begin{verbatim}
AA^{-1} = A^{-1}A =
\begin{bmatrix} 1 & 0 \\ 0 & 1 \end{bmatrix}
\end{verbatim}

.

\begin{verbatim}
# Діагональна матрицяНехай A — квадратна матриця порядку n і нехай всі її власні значення  \lambda_1  ,  \lambda_2  ...  \lambda_n  різні. Побудуємо матрицю P так, щоб у неї були *стовпці* координати власних векторів (k-й стовпець містить власний вектор  X_k  , що відповідає власному значенню  \lambda_k  ), тобто
\end{verbatim}

\begin{verbatim}
P =
\begin{bmatrix} X_1 & X_{\tilde{2}} & \dots & X_n \\ X_{1} & X_{2} & \dots & X_n \end{bmatrix}
=
\begin{bmatrix} x_{11} & x_{1\tilde{2}} & \dots & x_{1n} \\ x_{21} & x_{22} & \dots & x_{2n} \\ \vdots & \vdots & \ddots & \vdots \\ x_{n1} & x_{n\tilde{3}} & \dots & x_{nn} \end{bmatrix}
\end{verbatim}

.

Тогда матрица Л, определяемая соотношением

\begin{verbatim}
\Lambda = P^{-1}AP,
\end{verbatim}

где \texttt{P\^{}\{-1\}} --- обратная к матрице P, будет диагональной
матрицей вида

\begin{verbatim}
\Lambda =
\begin{bmatrix} \lambda_1 & & & \\ & \lambda_2 & 0 & \\ & & \ddots & \\ & & & \lambda_n \end{bmatrix}
\end{verbatim}

.

Например, у матрицы

\begin{verbatim}
A =
\begin{bmatrix} 0 & 8 \\ 2 & 0 \end{bmatrix}
\end{verbatim}

собственные числа равны \texttt{\textbackslash{}lambda\_\ 1\ =\ 4} и
\texttt{\textbackslash{}lambda\_2\ =\ -4} , а собственные векторы
соответственно

\begin{verbatim}
X_1 =
\begin{bmatrix} 2 \\ 1 \end{bmatrix}
in  X_2 =
\end{verbatim}

\pandocbounded{\includegraphics[keepaspectratio]{images/_page_221_Picture_9.jpeg}}

Рис. 30.1. Қолеблющаяся мембрана (гиперболическая смешанная задача).где

\begin{verbatim}
\Delta U = U_{rr} + \frac{1}{r} U_r + \frac{1}{r^2} U_{\theta\theta}.
\end{verbatim}

\begin{verbatim}
Зверніть увагу, що тут ми вимагаємо, щоб константа розділення була додатною (тому позначимо її  \lambda^{2-1}  )). Лише в цьому випадку функції T(t) будуть періодичними.На следующем шаге мы решим уравнение Гельмгольца, но сначала разберемся с граничным условием для него. Чтобы найти его, подставим  u(r,  \theta, t) = U(r, \theta) T(t)  в граничное условие для мембраны
\end{verbatim}

\begin{verbatim}
u(1, \theta, t) = U(1, \theta) T(t) = 0, \quad 0 < t < \infty,
\end{verbatim}

или

\begin{verbatim}
U(1, \theta) = 0.
\end{verbatim}

Следовательно, для того чтобы найти форму фундаментальных колебаний
мембраны, необходимо решить задачу

\begin{verbatim}
\Delta U + \lambda^2 U = 0,
U(1, \theta) = 0.
\begin{cases} \Delta U + \mu u = 0 & \text{в круге,} \\ U = 0 & \text{на границе круга} \end{cases}
\end{verbatim}

\begin{verbatim}
при  \mu \le 0  имеет только тривиальное решение U=0. — Прим. ред.<sup>1)</sup> Поскольку краевая задачаЭто хорошо известная эллиптическая задача на собственные значения, и нам надо найти все  \lambda , при которых она имеет ненуленые решения. Решения  U\left(r,\,\theta\right)  описывают форму фундаментальных колебаний мембраны, а собственные числа  \lambda  являются нулями некоторых функций Бесселя и пропорциональны частотам этих колебаний.Итак, нам предстоит теперь решить задачу на собственные значения для уравнения Гельмгольца (эта задача очень важна и сама по себе). Будем решать ее тем же методом, что и другие линейные однородные уравнения с нулевыми граничными условиями, а именно методом разделения переменных.# Решение задачи на собственные значения для уравнения ГельмгольцаЧтобы решить задачу
\end{verbatim}

\begin{verbatim}
\begin{array}{ll} (\mathsf{Y}\mathsf{H}\mathsf{\Pi}) & \Delta U + \lambda^2 U = 0, \\ (\mathsf{\Gamma}\mathsf{Y}) & U(1, \theta) = 0, \end{array}
\end{verbatim}

подставим

\begin{verbatim}
U(r, \theta) = R(r) \Theta(\theta)
\end{verbatim}

в задачу Гельмгольца. Сделав это, получаем

\begin{verbatim}
r^2R'' + rR' + (\lambda^2r^2 - n^2)R = 0
\end{verbatim}

\begin{verbatim}
(уравнение Бесселя),  R(1) = 0  ,  \Theta'' + n^2\Theta = 0  (Зроби все сам). Зверніть увагу, що ми обрали константу розбиття  n^2  ,  n=0,. \ 1,\ 2,\ \ldots  тому, що хочемо, щоб функції  \Theta\left(\theta\right)  були періодичними з періодом  2\pi  . Очевидно, що форма мембрани є періодичною функцією  \theta  . Отже, щоб розв'язати задачу Гельмгольца, потрібно розв'язати два звичайних диференціальні рівняння
\end{verbatim}

\begin{verbatim}
r^2R'' + rR' + (\lambda^2r^2 - n^2) R = 0
\end{verbatim}

\begin{verbatim}
,  0 < r < 1  ,  R(0) < \infty  (физическое ограничение),  R(1) = 0  ,  \Theta'' + n^2\Theta = 0  .# Уравнение БесселяУравнение
\end{verbatim}

\begin{verbatim}
r^2R'' + rR' + (\lambda^2r^2 - n^2)R = 0
\end{verbatim}

\begin{verbatim}
жорошо известно в теории обыкновенных дифференциальных уравнений. Это так называемое уравнение Бесселя. Оно имеет двалинейно-независимых решения  R_{1}(r) = A J_{n}(\lambda r)  функция Бесселя *первого* рода *п*-го порядка,  R_{2}(r) = BY_{n}(\lambda r)  функция Бесселя второго рода n-го порядка,и, следовательно, общее решение этого уравнения представимо в виде
\end{verbatim}

\begin{verbatim}
R(r) = AJ_n(\lambda r) + BY_n(\lambda r).
\end{verbatim}

\begin{verbatim}
Зверніть увагу, що рішення залежать від параметрів n і  \lambda  , включених у рівняння. Деякі графіки функції Бесселя показані на рис. 30.2. Щоб визначити функції  J_ n(\lambda r)  і  Y_n(\lambda r)  , можливо
\end{verbatim}

\pandocbounded{\includegraphics[keepaspectratio]{images/_page_223_Figure_8.jpeg}}

\pandocbounded{\includegraphics[keepaspectratio]{images/_page_223_Figure_9.jpeg}}

\begin{verbatim}
Рис. 30 2 Функции Бесселя. a — функции Бесселя первого рода;  \pmb{\delta}  — функции Бесселя второго рода.використовуйте метод Фробеніуса, який дозволяє знаходити R(r) у вигляді силових рядів. Цей метод містить два лінійно незалежні ряди ступенів  J_n(\lambda r)  та  Y_n(\lambda r)  . Оскільки функція  Y_n(\lambda r)  не обмежена в r=0, ми обираємо розв'язок у вигляді
\end{verbatim}

\begin{verbatim}
R(r) = AJ_n(\lambda r).
\end{verbatim}

\begin{verbatim}
Последний шаг в определении  R\left( r\right)  связан с использованием граничного условия  R\left( \mathbf{l}\right) =0  для определения всех  \lambda  (в данныйТаблица 30.1| Корни | уравнений. |  J_n(r) = 0  |
\end{verbatim}

\textbar-------\textbar------------\textbar--------------\textbar{}

\textbar-------\textbar------------\textbar--------------\textbar\textbar{}
Γ \textbar{} 1 \textbar{} n \textbar{} \textbar{} \textbar{} \textbar{}
\textbar{} \textbar{} \textbar{}

\textbar---\textbar---\textbar-------\textbar-------\textbar-------\textbar-------\textbar-------\textbar--\textbar--\textbar{}

~~\textbar{} \textbar{} 0 \textbar{} 1 \textbar{} 2 \textbar{} 3
\textbar{} 4 \textbar{} \textbar{} \textbar{}

~~\textbar{} 1 \textbar{} 2,40 \textbar{} 3,83 \textbar{} 5,13
\textbar{} 6,38 \textbar{} 7,59 \textbar{} \textbar{} \textbar{}

ı \textbar{} 2 \textbar{} 5,52 \textbar{} 7,02 \textbar{} 8,42
\textbar{} 9,76 \textbar{} 11,06 \textbar{} \textbar{} \textbar{}

ľ \textbar{} 3 \textbar{} 8,65 \textbar{} 10,17 \textbar{} 11,62
\textbar{} 13,02 \textbar{} 14,37 \textbar{} \textbar{} \textbar{}

~~\textbar{} 4 \textbar{} 11,79 \textbar{} 13,32 \textbar{} 14,80
\textbar{} 16,22 \textbar{} 17,62 \textbar{} \textbar{} \textbar{}

~~\textbar{} 5 \textbar{} 14,93 \textbar{} 16,47 \textbar{} 17,96
\textbar{} 19,41 \textbar{} 20,83 \textbar{} \textbar{} \textbar{}

~~\textbar{} . \textbar{} • \textbar{} \textbar{} • \textbar{}
\textbar{} • \textbar{} \textbar{} \textbar{}

~~\textbar{} \textbar{} \textbar{} • \textbar{} \textbar{} • \textbar{}
• \textbar{} \textbar{} \textbar{}

\begin{verbatim}
|   | • | •     | •     | •     | •     | •     |  |  |момент нас не интересует коэффициент A). Подставляя R (1) = 0, в R (r) =  J_n  (  \lambda r  ), получаем  J_n  (  \lambda  ) = 0. Другими словами, для того чтобы функция R (r) обращалась в нуль на границе круга, мы должны выбрать константу разделения так, чтобы она была корнем уравнения  J_n  (r) = 0,  \tau  . e.
\end{verbatim}

\begin{verbatim}
\lambda = k_{nm}
\end{verbatim}

\begin{verbatim}
где  k_{nm}  — m-й корень уравнения  J_n(r) = 0  .Таблиці цих коренів добре відомі, але їх можна обчислити на комп'ютері. Значення перших кількох коренів наведені в Таблиці 1. 30.1. Знаючи ці корені, ми можемо одразу побудувати розв'язок задачі граничного значення для рівняння Гельмгольца. Якщо власні значення  \lambda = k_{nm}  , то власні функції  U_{ nm}(r)  визначаються формулою
\end{verbatim}

\begin{verbatim}
U_{nm}(r, \theta) = J_n(k_{nm}r) [A \sin(n\theta) + B \cos(n\theta)].
\end{verbatim}

\begin{verbatim}
На рис. 30.3 изображены некоторые из этих функций для различных значений n и m. Общая форма функции  U_{nm}(r,\theta)  не зависит от значений констант A и B. Эти константы влияют только на амплитуду колебаний и расположение линии узлов относительно  \theta=0  .Каждая функция представляет собой фундаментальное колебание круглой мембраны с частотой
\end{verbatim}

\begin{verbatim}
f_{nm} = k_{nm}c/2\pi.
\end{verbatim}

Эти частоты мы нашли из решений

\begin{verbatim}
T_{nm}(t) = A \sin(k_{nm}ct) + B \cos(k_{nm}ct)
\end{verbatim}

\section{уравнения}\label{ux443ux440ux430ux432ux43dux435ux43dux438ux44f}

\begin{verbatim}
T'' + k_{nm}^2 c^2 T = 0.
\end{verbatim}

\begin{verbatim}
Интересно отметить, что отношение частоты колебания  U_{nm}(\mathbf{r}, \mathbf{\theta})  к частоте основного колебания  U_{nt}(\ma
\end{verbatim}

\begin{verbatim}
sU(x) = -V \frac{dU}{dx}, \quad 0 < x < \infty,
\begin{cases} u(R_1, \theta) = g_1(\theta), \\ u(R_2, \theta) = g_2(\theta). \end{cases}
U(0) = P/s.
\begin{cases} 2/3, & n=1, \\0 & \text{для всіх інших } n, \\ d_n=\begin{cases} -2/3, & n=1, \\0 & \text{для всіх інших} n. \end{cases} \end{cases}
H(\xi) =
\begin{cases} 1, & 0 \le \theta < \pi, \\ 0, & \pi \le \theta < 2\pi. \end{cases}
\begin{cases} 0, & \xi < 0, \\ 1, & \xi \geqslant 0. \end{cases}
\end{verbatim}

\begin{verbatim}
INL1448X (\Gamma Y)   u(1, \varphi) = \cos(3\varphi)  .Используя тригонометрию, попытайтесь выразить  \cos{(3\phi)}  через  \cos{\phi}, \, \cos^2{\phi}, \, \cos^3{\phi}, \, a  затем скомбинируйте их так, чтобы по-лучилось разложение
\end{verbatim}

\begin{verbatim}
\cos(3\varphi) = a_0 P_0(\cos\varphi) + a_1 P_1(\cos\varphi) + \dots
\end{verbatim}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\setcounter{enumi}{4}
\tightlist
\item
  Решите задачу Дирихле в шаре
\end{enumerate}

\begin{verbatim}
\Delta u = 0, \quad 0 < r < 1, \quad 0 \le \varphi \le \pi, \quad 0 \le \theta < 2\pi.
u(1, \varphi) =
\begin{cases} 1, & 0 \le \varphi \le \pi/2, \\ -1, & \pi/2 < \varphi \le \pi. \end{cases}
\end{verbatim}

В этой задаче верхняя полусфера нагрета (+1), а нижняя охлаждена (-1).6.
Найдите решение задачи Дирихле в шаре(УЧП)

\begin{verbatim}
\Delta u = 0
\end{verbatim}

\begin{verbatim}
,  1 < r < \infty  ,  0 \le \varphi \le \pi  ,  0 \le \theta < 2\pi  ,  (\Gamma Y)   u(1, \varphi) = 1 + \cos \varphi  .Проверьте ответ.Лекция 36# НЕОДНОРОДНАЯ ЗАДАЧА ДИРИХЛЕ (ФУНКЦИЯ ГРИНА)МЕТА ЛЕКЦІЇ: Показати, як гетерогенну задачу Діріхле можна розв'язати за допомогою функції Гріна (вихідної функції). У цьому важливому методі права частина рівняння розглядається як певна вхідна дія і розкладається на неперервний набір дельтаподібних джерел, розподілених по заданій площі. Потім визначається відповідь системи на кожне таке джерело (функція Гріна), і всі відповіді підсумовуються (інтегровані). У результаті ми отримуємо повне розв'язання проблеми.Достаточно общей задачей прикладной математики является определение потенциала в некоторой области пространства, если задано распределение источников f(x, y) енутри этой области. В электростатике потенциал (в вольтах) в области D обусловлен распределением зарядов с плотностью f(x, y) по этой области. Типичная задача — определение потенциала в круге в двумерном случае.В такой задаче потенциал должен удовлетворять уравнениюПуассона (например, с нулевыми граничными условиями)(36.1) (УЧП)
\end{verbatim}

\begin{verbatim}
u_{rr} + \frac{1}{r} u_r + \frac{1}{r^2} u_{\theta\theta} = f(r, \theta), \quad 0 \le r < 1, \quad 0 \le \theta < 2\pi,
\end{verbatim}

\begin{verbatim}
(ГУ)  u(1, \theta) = 0, \quad 0 \le \theta \le 2\pi.  Ми не випадково обрали нульові умови. Якщо б ми хотіли розв'язати задачу в загальному випадку, з ненульовими граничними умовами та неоднорідним рівнянням, то внесок неоднорідних граничних умов можна було б знайти за допомогою формули інтегралу Пуассона з лекції 33.Чтобы получить хоть какое-то представление о неоднородных дифференциальных уравнениях, давайте рассмотрим графически решение следующей задачи для уравнения Пуассона:(УЧП)
\end{verbatim}

\begin{verbatim}
\Delta u = -q
\end{verbatim}

\begin{verbatim}
,  0 < r < 1  ,  0 \le \theta \le 2\pi  (  q  —положительная константа),(HY)
\end{verbatim}

\begin{verbatim}
u(1, \theta) = 0, \quad 0 \le \theta \le 2\pi.
\end{verbatim}

\begin{verbatim}
На межі потенціал (або температура, якщо ви бажаєте) утримується на нулі, а лапласіан дорівнює q у всіх точках області. Оскільки  \Delta u\left(p\right)  слугує мірою різниці між функцією  u\left(p\right)  і її середнім, рівняння Пуассона стверджує, що поверхня  u\left(r\right)  завжди опукла вниз. Інакше кажучи, вона виглядатиме як тонка мембрана з нерухомим краєм, яка здувається зверху вниз потоком повітря.Якщо права частина рівняння f(x, y) змінюється від точки до точки, то опуклість також буде змінною функцією точки цієї області.Перейдем теперь к главной части нашей лекции: определению функции Грина и решению уравнения (36.1).Сначала, однако, мы должны ввести понятие потенциала точеных источников и стоков.# Потенциалы точечных источников и стоковЩоб розв'язати неоднорідне лінійне рівняння, достатньо розв'язати це рівняння за допомогою точкового джерела, оскільки розв'язок задачі в загальному випадку можна знайти, підсумувавши внески з точкових джерел. Наше завдання — визначити потенціал, який створює точкове джерело (або поглинач) у певній області. Ці джерела можна інтерпретувати по-різному. У теорії теплопровідності джерело можна розглядати як точку, де походить тепло, а дренаж — як точку, де поглинає тепло. В електростатиці джерело можна розглядати як окремий позитивний заряд (протон), а дренаж — як окремий негативний заряд (електрон). У будь-якому разі, незалежно від інтерпретації, ми знайдемо двовимірний потенціал і,создаваемый единичным точечным источником (поиск трехмерного потенциала мы оставляем читателю в качестве упражнения).Припустимо, що єдине точкове джерело +q розташоване в початку Clear that heat (або щось інше)будет вытекать из области вдоль радиальных линий, и, следовательно, для везничны полного потока, протекающего через окружность радиуса r можно записатьПолный поток, вытекающий через окруж-HOCTD
\end{verbatim}

\begin{verbatim}
= -\int_{0}^{2\pi} u_{r}(r) r d\theta = -2\pi r u_{r}(r).
\end{verbatim}

Но полный поток должен совпадать с количеством тепла, созданного внутри
круга (закон сохранения), т. е.

\texttt{-2\textbackslash{}pi\ r\ \textquotesingle{}u\_r\textquotesingle{}(r)\ =\ q.\$\$\ Решая\ это\ диф\ реренциальное\ уравнение\ относительно\ функции\ \textquotesingle{}u(r)\textquotesingle{},\ получаем\ Р\ И\ С}

\end{document}
