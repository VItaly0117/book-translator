# Лекція 28. КАНДИДАТНИЙ ФРАГМЕНТ З ДОНОРА

Це не чиста окрема лекція, а збережений фрагмент з донора `6977–7012`, який, за планом, уже змішаний з матеріалом лекції 28.

Робоча назва лекції 28:
`Нелінійні рівняння першого порядку (закони збереження)`.

Що важливо:
- окремий чистий заголовок лекції 28 в донорі втрачено;
- цей файл не слід вставляти як готову лекцію без ручної перевірки;
- його слід використовувати разом із змішаним current-блоком після лекції 27.

Нижче збережено сам фрагмент без творчого редагування.

---

$$

\begin{split} & (\text{NU}) \quad u_t + u^2 u_x = 0, \quad -\infty < x < \infty, \quad 0 < t < \infty, \\ & (\text{NU}) \quad u_t(x, 0) = \begin{cases} 0, & x < 0, \\ x, & 0 \leqslant x. \end{cases}

$$

Какова здесь связь между потоком и плотностью? Совнадает ли поведение полученного решения с тем, что вы ожидали? Сравните решения задачи 1 и задачи 2.

3. Предположим, что невязкая жидкость течет по трубе и при этом просачивается через стенки по закону F(u) = ku (г/см·с). Закон сохранения (скорее закон изменения) в этом случае записывается в виде

$$

u_t + f_x = -F(u)

$$.

Найдите решение этого уравнения, если f(u) = u и  $\varphi(x) = u$  (x, 0) (начальное распределение задано в общем виде). Дайте интерпретацию полученного решения.

4. Найдите решение предыдущего уравнения, если потери определяются функцией F(x, t) = 1/x. Проверьте полученное решение. Ясен ли вам его физический смысл?

5. Проверьте, что функция и, неявно заданная соотношением

$$

u = \varphi[x - g(u) t],

$$

является решением нелинейной задачи

$$

(\text{NU}) \quad \begin{array}{ll} u_1 + g(u) u_x = 0, \\ (\text{NU}) & u(x, 0) = \varphi(x). \end{array}

$$
