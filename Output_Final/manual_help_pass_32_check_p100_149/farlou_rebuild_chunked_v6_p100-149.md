

![](images/_page_101_Picture_7.jpeg)

Фіг. 13.2. Схематичне представлення задач теплового виробництва: a — це тепловий потік посередині, (0, t) < 0 i називається, а (0, t) > 0; b — резервуар підлога, який не викривлений знизу зверху з значеннями x.

Стержень має нульову орієнтацію температури (див. рис. 13.2). Наша мета означає температуру поля посеред річки і в перший рік. Іншими словами, нам потрібно розв'язати задачу

(13.6)

$$
\begin{aligned}
(\text{UBP})\quad & u_t = u_{xx}, && 0 < x < \infty,\; 0 < не < \infty, \\
(\Gamma \text{Y})\quad & u_x(0,t) - u(0,t) = 0, && 0 < t < \infty, \\
(\text{NU})\quad & u(x,0) = u_0, && 0 < x < \infty.
\end{aligned}
$$

Čob rozviýti ce zavdannya, meni vidmo vidvoríti Laplace u minlivu t. Vidnovíti povagu, chto možhna bulo zupiníti perevodennya Laplace na zminu x, zminiti z 0 na$\infty$. Обробка множення

Ми бачимо диференціальний рівень зміни X.

(13.7)

$$

(ODY) \quad sU(x) - u_0 = \frac{d^2U}{dx^2}, \quad 0 < x < \infty,

$$

(\Gamma Y) \quad \frac{dU}{dx}(0) = U(0)

$$

(Ми відтворили lishe rivnennya і розум на межі, а не cob.) Диференціальний рівень іншого порядку дорівнює одному граничному розуму при x=0. Na spravdi isnuje esche odin granichna umova. Бути побаченим фізичними світами і бути фізичним як$U(x) \to 0$, якщо$x \to +\infty$. Для спрощення позначення ми опустили параметр s аргументів функції U(x) всюди, що дорівнює рівню (13.7) диференціальних рівнів лише x.

Щоб розвинути задачу (13.7), запишіть горизонтальне рішення (однорідний + гетерогенний партисипативний розвиток) TAC:

$$

U(x) = c_1 e^{V_{5x}} + c_2 e^{-V_{5x}} + u_0/s.

$$

Представляючи кордон на межі (13.7), можна побачити сталість$c_1$ та $c_2$(одразу розуміло, що$c_3 = 0$ ,

![](images/_page_102_Figure_7.jpeg)

Фіг. 13.3. Граф світового інтегрування (eri) та комплементарної функції (eric).

Крім того, температура підвищується значно більше, ніж координати X). Ім'я Господнє$c_2$Z-межа величини при x=0, можна знайти у фізичній формі для$U\left(x\right)$ (13.8)

$$

U(x) = u_0 \left\{ \frac{e^{-V \widehat{s} x}}{s(V \widehat{s} + 1)} \right\} + \frac{1}{s}.

$$

Залишається зробити останній крок. Щоб визначити температурне поле, потрібно обчислити $u\left(x,\,t\right)$ $$

u(x, t) = \mathcal{L}^{-1}[U(x, s)]

$$

(ми перейшли до запису всіх аргументів функції U(x, s)). Щоб перетворити реконструкції Лапласа, ми зробили столи і принесли їх на стіл. U rezul'tati mi otrýmýemo

(13.9)

$$

u(x, t) = u_0 - u_0 \left[ \text{eric} \left( \frac{x}{2} \sqrt{t} \right) + \text{eric} \left( \sqrt{t} + \frac{x}{2} \sqrt{t} \right) e^{x+t} \right],

$$

де

$$

\operatorname{eric}(x) = \frac{2}{\sqrt{\pi}} \int_{x}^{\infty} e^{-\xi^{2}} d\xi

$$

Її функція, яка є додатковою до світової інтеграції. Графіки функції показані на рис. 13.3. Аналіз PH, C

![](images/_page_103_Figure_7.jpeg)

13.4. Температура в центрі заповненого центру в момент розриву.

Я вимагатиму трохи більше влади. Фіг. (13.4) представляє результати чисельного аналізу, виконаного на комп'ютері, оснащеному плоттером.

# ПРИМІТКИ

- 1. Відтворення Лапласа також можна звести до неоднорідного диференціального рівня з частковими множниками (для модифікації методу диференціації різних рівнів можна підняти до одного рівня), або можна довести до рівня різних множників (метод диференціації можна підняти до рівня коефіцієнтів заміщення). Таблиця 1. 13.2 Сфера має два методи.
- 2. Для досягнення цих і межових відносин також проводиться трансформація Ганкеля і Мелліна, а також трансформація Лапласа в одному аспекті. Версія формулювання оперативного множника формули Лапласа

$$

\mathscr{L}[y'] = s\mathscr{L}[y] - y(0),

$$

Таблиця 13.2 Диференціація методів розділення перетворення та заміни Лапласа

| | Метод | |
|-------------------------|---------------------------|----------------------------------|
| | <br>Оновлення Лапласа | <br><b>Розділ |</b>
| Гетерогенний ППП | Отже | Отже |
| Гетерогенний ГІ | Отже | Отже |
| Зміни шляхи | Отже | de' |
| Нелінійні ривняня | Отже | Отже |

а зведення Ганкеля і Мелліна замінює диференціальний оператор на множник. Застосовано для трансформації Ганкеля, створеного за формулою

$$

H[y] = \int_{0}^{\infty} r J_{o}(\xi r) y(r) dr,

$$

Чесна промова

$$

H\left[y''\left(r\right)+\frac{1}{r}\ y'\left(r\right)\right]=-\xi^{2}H\left[y\right].

$$

З цим рангом можливо розробити спеціальний диференціальний рівень з різними коефіцієнтами (рівень Бесселя). 3. Перетворення Лапласа у зміну t можна інтерпретувати як проєкт області xt на осі x, яку можна простежити до U c o m u v i

![](images/_page_104_Figure_9.jpeg)

У випадку початку кордон, а також регіони кордону та кордону трансформуються у новий диференціальний рівень і нову межу (див. Diagramu vische).

# ЗАВДАННЯ

1. Переконайця, що навчання пристоженість перевоженності частковою похідною$u_t$У:

$$

\mathscr{L}\left[u_{t}(x, t)\right] = sU(x, s) - u(x, 0).

$$

2. Vikoristati perevorennya Laplace dlya razv'yazannya problemi Koshi

$$

\begin{aligned}
(\text{UBP})\quad & u_t = \alpha^2 u_{xx}, && -\infty < x < \infty,\ 0 < t < \infty, \\
(\text{NU})\quad & u(x,0) = \sin x, && -\infty < x < \infty.
\end{aligned}

$$

3. За допомою переворення Лаплас за змінного$t$Завдання розв'язати

$$

\begin{aligned}
(\text{UBP})\quad & u_t = u_{xx}, && 0 < x < \infty,\ 0 < t < \infty, \\
(\text{GU})\quad & u(0,t) = \sin t, && 0 < t < \infty, \\
(\text{NU})\quad & u(x,0) = 0, & 0 \leqslant x < \infty.
\end{aligned}

$$

Дайте фізичне тлумачення завдання.

4. Розробити задачу з граничними значеннями

(ОДА)

$$

\frac{d^2U}{dx^2} - sU = A, \quad 0 < x < 1,

$$

(\Gamma Y) \quad

$$

\begin{cases} \frac{dU}{dx} (0) = 0 \\ U(1) = 0. \end{cases}

$$

Лекція 14

# ПРИНЦИП ДЮАМЕЛЯ

МЕТА ЛЕКЦІЇ: Показати, як переклад Лапласа можна використовувати для ідентифікації характеристик диференціальних рівнів. Зокрема, за допомогою алгебранічного перетворення образів Лапласа, розвиток рівня частково обгорнутого може бути під впливом принципу погляду Дюамеля. Інтерпретація цього принципу в теорії світових диференціальних рівнів має використовуватися для розвитку співаючого UPP.

Технології.

Давайте подивимось, як трансформація Лапласа впливає на розвиток часткових диференціальних ліній, що дозволяє нам розкрити фізичну сутність різних розвитків. За допомогою трансформації Лапласа ми вже розробили принцип Дюамеля. По-перше, що необхідно, щоб звернутися до принципу Дюамеля, ми маємо задачу, яка часто є проблемою

# Teploprovidniś u strizhni, кінці якого підтримують на задаій temperatureі

Крім того, часто необхідно знаходити температуру в центрі регіону, тобто в центрі регіону, щоб температура між регіонами змінювалася від години до закону співу (граничні умови, години пару).

На лівому боці світу,$u(0,t)=0$, а праворуч температура встановлена на функцію$f(t)$.

Фіг. 14.1. Граничні умови, щоб лежати годину.

Під час нанесення можна побачити термозоляційний стержень, праві кінети якого підтримуються f(t) (чудова рисунок 14.1). Потреба задачі

(14.1)

$$

u_{t} = u_{xx}, \quad 0 < x < 1, \quad 0 < t < \infty,

$$ (\Gamma Y) \quad \begin{cases} u(0, t) = 0, \\ u(1, t) = f(t), \end{cases} \quad 0 < t < \infty,$$

(HY) \quad u(x, 0) = 0, \quad 0 \le x \le 1.

$$

Рекомендується, щоб розробка цього завдання (14.1) була простою, ми знатимемо, як розв'язати просту задачу, і температура у вікні стане:

Якщо задачі (14.1) і (14.2)) розв'язані за одну годину за допомогою відтворення Лапласа, то досягається значущий результат: задача (14.1) розв'язується через її розвиток (14.2).

Тому, коли я розв'язую проблемні завдання одразу, я бачу

# Legke zavdannya (14.2) (Postiyni soldati)

Stosuvannya perevorennya -Laplace do zadati (14.2)

$$

\frac{d^2W}{dx^2} - sW(x) = 0,

$$ W(0) = 0,$$

W(1) = 1/s

$$.

Основний ODU

$$

W(x, s) = \frac{1}{s} \left[ \frac{\sinh(x \sqrt{s})}{\sinh(\sqrt{s})} \right].

$$

Загорнуті зміни

$$

w(x,t)=x+\frac{2}{\pi}\sum_{n=1}^{\infty}
\frac{(-1)^n}{n}e^{-(n\pi)^2t}\sin(n\pi x).

$$

Розробка завдань із постідентифікаційними межами розуму.

# Важке завдання (14.1) (Солдаті, ще залежат від часу)

Styasovannya pere-transitennya Laplace v problemi (14.1)

$$

\frac{d^2U}{dx^2}-sU(x)=0,\qquad U(0)=0,\qquad U(1)=F(s).

$$

$$

U(x, s) = F(s) \left[ \frac{\sinh(x \sqrt{s})}{\sinh(\sqrt{s})} \right].

$$

Ділимо і множимо на z

$$

U(x, s) = F(s) \left\{ s \left[ \frac{\sinh(x \sqrt{s})}{s \sinh(s)} \right] \right\}.

$$

Vicorystvovy spivvidnosnya$\mathscr{L}[w_t] = sW - w(x, 0),$Див.

$$

U(x, s) = F(s) \mathcal{L}[w_t].

$$

Відповідно,

$$

u(x, t) = \mathcal{L}^{-1} \{ F(s) \mathcal{L}[w_t] \} =

$$

= \mathcal{L}^{-1} [ F(s) ] * \mathcal{L}^{-1}[w_t] =

$$

= f(t) * w_t(t) =

$$

= \int_0^t w_t(x, t-\tau) f(\tau) d\tau =

$$

(написано шляхом інтегрування частин)

$$

= \int_{0}^{t} w(x, t-\tau) f'(\tau) d\tau + f(0) w(x, t).

$$

(Зв'язок задачі з ГІ з годинним депозитом виконується через зв'язок задачі з постійним ГІ

У задачах з константою

Крім того, ми обговорювали зв'язок завдань із прикордонними свідомостями, які можна змінити за кілька годин, через розвиток проблем із постійними прикордонними свідомостями. Використовуються такі формули:

(14.3)

# Лекція 15. КОНВЕКТИВНИЙ ЧЛЕН$u_x$ДЛЯ ЗАВДАНЬ ДИФУЗІЇ

МЕТАЛЕКЦІЇ: Щоб показати, як термін$u_x$у задачі дифузії

$$

u_t = Du_{xx} - Vu_x

$$

Дифузійний член. Конвективний член.

Рівняння конвективної дифузії описує одночасно дифузію та перенесення речовини потоком. Відносна роль цих двох механізмів визначається коефіцієнтом дифузії $D$ і швидкістю потоку $V$.

Оскільки конвекція зумовлена рухом речовини разом із середовищем, природно перейти до системи координат, що рухається разом із потоком. У такій системі конвективний член зникає, а отримане рівняння можна розв'язати вже відомими методами. Повернувшись до нерухомої системи координат, дістаємо розв'язок початкової задачі.

Досі ми розглядали теплопровідність або дифузію в одновимірних системах. Тепер розглянемо задачу про концентрацію речовини, що виходить із землі та потрапляє у висхідні потоки повітря. У цьому випадку разом із дифузією треба враховувати конвективне перенесення потоком зі швидкістю $V$. Наша мета — вивести рівняння конвективної дифузії

$$

u_t = Du_{xx} - Vu_x

$$

Для виведення рівняння конвективної дифузії потрібні два основні факти.

1. Потік через конвекцію.

Конвективний потік речовини вправо через поперечний переріз у точці $x$ дорівнює $Vu(x,t)$, де $V$ — швидкість середовища, а $u(x,t)$ — концентрація речовини (рис. 15.1).

![](images/_page_112_Picture_4.jpeg)

Фіг. 15.1. Кількість речовини, що переноситься за одиницю часу через переріз у точці $x$ завдяки конвекції, дорівнює $Vu(x,t)$.

Рис. 15.2. Задача конвективної переносності.

2. Потік через дифузію.${\it Диффузионный поток}$речовина зліва направо через поперечний розріз у цій точці дорівнює —$Du_x(x, t)$, де D — коефіцієнт дифузії.

Підставляючи ці потоки в закон збереження з лекції 3, отримуємо основне диференціальне рівняння

$$

u_t = Du_{xx} - Vu_x.

$$

Щоб зрозуміти вплив конвективного члена, спочатку розглянемо лише механізм переносу. Типовий приклад — задача про скидання речовини в річку зі швидкістю течії $V$. Нехай $x$ — відстань униз за течією від місця скидання. Якщо речовина не дифундує у воді, то її концентрація $u(x,t)$ задовольняє задачу (рис. 15.2):

(УЧП)

$$
u_t = -V u_x, \qquad 0 < x < \infty,\quad 0 < t < \infty,
$$

(ГУ)

$$
u(0,t)=P,
$$

(НУ)

$$
u(x,0)=0.
$$

Здесь $P$— Це postіyna kontsentsııa na mistsi skidu, a pochatkova umova označajae, scho na pochatkovomu etapi richka chista.

Перед розв'язуванням проблем необхідно подумати, яке може бути рішення. Очевидно, що річки закриті

(кілкіст речовини у воі) спочаку буде zero, а потім, підтримувана контой х = 0, рухатить за течією за течіею зі швидкістю В. Щоб переконатья в цьому, розв'яжмо problem (15.1). Рівень (15.1) не є льоном, а межовий розум також з льону, це завдання можливо розвивати методом розділення взаємозамінних або інтегруючих перетворень. Однак x-координата тут не розмежована, і метод поділу змін не буде зупинено. Vikoristite transformaciju Laplace u chasi t.

#### Роз'язок ривнянні транспорту за допомомою перетворенної Лаплас

Використання перетворення Лапласа

$$

U(x) = \int_{0}^{\infty} u(x, t) e^{-st} dt

$$

Задача конвективного транспорту (15.1) може бути зведена до вигляду$sU(x) = -V \frac{dU}{dx}, \quad 0 < x < \infty$ $U(0) = P/s$ .

Розв'язуючи цю дуже просту задачу Коші, ми отримуємо$U(x) = Pe^{-(sx/V)}/s$ .

Вікористичні таблиці загорнутої реконструкції, ми знаходимо зв'язки з задачею$u(x, t) = \mathcal{L}^{-1}[U] = PH(t-x/V),$де H (ξ) — покрокова функція Хевісайда

$$

H(\xi) = \begin{cases} 0, & \xi < 0, \\ 1, & \xi \geqslant 0. \end{cases}

$$

Зв'язок наших задач можна надати такої форми:

$$

u(x, t) = \begin{cases} 0, &t < x/V, \\ P, & t \ge x/V. \end{cases}

$$

Звісно, все просто! Не складати, не кидати її на конвеєр і не відхиляти назад, ніби вона ось-ось обвалиться. Одна з ситуацій зграї вже знаходиться посеред будинку. Що зрозуміти, що відбуваєє с рухому хвлюя у присіні діфії, розв'яжмо

Наступне завдання

(15.2)

$$
u_t = Du_{xx} - Vu_x, \quad -\infty < x < \infty,
$$

$$
u(x, 0) = 1 - H(x), \quad -\infty < x < \infty.
$$

де, як звичайно, $H(x)$ — функція Гевісайда. Початковий розподіл концентрації показано на рис. 15.4.

![](images/_page_114_Figure_4.jpeg)

Рис. 15.3. Конвективна хвиля: а — фронт домішки; $\delta$ — одиниця довжини попереду фронту хвилі.

Зверніть увагу, що в задачі (15.2) межу відсунено до $-\infty$, тобто тепер розглядається задача Коші. Її можна розв'язувати перетворенням Лапласа за часом або перетворенням Фур'є за просторовою змінною.

![](images/_page_114_Figure_7.jpeg)

Рис. 15.4. Початкові умови в задачі конвективної дифузії.

Однак у цьому випадку цікавіше зробити інакше: перейти до системи координат, що рухається зі швидкістю $V$. Іншими словами, замість системи координат, прив'язаної до берега річки, розглянемо систему, що рухається разом із фронтом домішки. Математично це означає заміну просторової координати $x$ новою координатою $\xi=x-Vt$.

Тепер зрозуміло, що

коли $\xi = 0$, ми перебуваємо на фронті поширення домішки.

коли $\xi = 1$, ми на одну одиницю довжини попереду фронту.

коли $\xi = -1$, ми на одну одиницю довжини позаду фронту.

Наше завдання — трансформувати початкову задачу з NU

$$

\begin{array}{lll} (\mathrm{Y} + \Pi) & u_t = Du_{xx} - Vu_x, & -\infty < x < \infty, \\ (\mathrm{H} \mathrm{Y}) & u(x, 0) = 1 - H(x), & -\infty < x < \infty, \end{array}

$$

u нова задача в системі координат Рухомії, розв'язат — нову задачу, а потім перенесіть розв'язки у старі координати (x, t). Spochatku zaminimo zminni (zaminimo nezalezhni zminni). Місце старих координат (x, t) та введення нових координат$(\xi, \tau)$За формулами

$$

\xi = x - Vt,

$$

$$

\tau = t.

$$

Слід зазначити, що листи$\tau$а t позначається Ведичною династією. Таке позначення вводиться з метою уніфікації

Плутанін у майбутніх формулах. Щоб записати диференціальний рівень у нових координатах (\ $, t), ми використаємо такі формули переходу;

$$

u_{t} = u_{\xi} \, \xi_{t} + u_{\tau} \, \tau_{t} = -V u_{\xi} + u_{\tau},

$$

$$

u_{x} = u_{\xi} \, \xi_{x} = u_{\xi},

$$

$$

u_{xx} = (u_{\xi})_{x} = u_{\xi\xi} \xi_{x} = u_{\xi\xi}.

$$

Функціональна діаграма на рис. 15.5 робить ці формули досить очевидними. Вона особливо корисна під час обчислення часткових похідних функції $u$ за новими змінними.

![](images/_page_115_Picture_14.jpeg)

Рис. 15.5. Діаграма, що ілюструє функціональну залежність змінних.

$\xi$ залежить і від $x$, і від $t$, тоді як $\tau$ залежить лише від $t$. Тепер підставимо знайдені вирази для $u_t$, $u_x$ і $u_{xx}$ у рівняння (15.2) та отримаємо

$$

-Vu_{\xi}+u_{\tau}=Du_{\xi\xi}-Vu_{\xi},

$$

звідки

$$

u_{\tau} = Du_{\xi\xi}

$$

.

Відповідно, нова задача з початковою умовою в першому$ \xi $  и  $ \tau $має вигляд

(УЧП) 

$$

u_{\tau} = Du_{\xi\xi}, \quad -\infty < \xi < \infty,

$$

(НУ)  $ u(\xi, 0) = 1 - H(\xi), \quad -\infty < \xi < \infty. $ 

Оскільки$ \xi = x $якщо t = 0, нові граничні умови збігаються зі старими. Цю задачу було розв'язано на лекції 12 за допомогою інтегрального перетворення Фур'є, а її розв'язок записано у вигляді

$$

u(\xi, \tau) = \frac{1}{2\sqrt{D\pi\tau}} \int_{-\infty}^{\infty} \varphi(\beta) e^{-(\xi-\beta)^2/4D\tau} d\beta,

$$

где  $ \phi(\beta) $— початкова умова. Отже, у нашому випадку,

$$

u(\xi, \tau) = \frac{1}{2\sqrt{D\pi\tau}} \int_{-\infty}^{0} e^{-(\xi-\beta)^2/4D\tau} d\beta.

$$

Після заміни

$$

\overline{\beta} = \frac{\xi - \beta}{2\sqrt{D\tau}}, \quad d\overline{\beta} = \frac{-1}{2\sqrt{D\tau}}d\beta

$$

Ми отримуємо фінальний результат

(15.3) 

$$

u(\xi, \tau) = \frac{1}{2} \left[ \frac{2}{\sqrt{\pi}} \int_{\frac{\xi}{\sqrt{D\tau}}}^{\infty} e^{-\overline{p}^{2}} d\overline{\beta} \right] = \begin{cases} \frac{1}{2} \left[ 1 + \operatorname{erf} \left( \frac{-\xi}{2\sqrt{D\tau}} \right) \right], & \xi < 0, \\ \frac{1}{2} \operatorname{erfc} \left( \frac{\xi}{2\sqrt{D\tau}} \right), & 0 \leq \xi. \end{cases}

$$

Графіки цього розв'язку для різних моментів часу показані на рис. 15.6. Останній крок — записати розв'язок нашої задачі в координатах x і t.

$$
\xi = x - Vt, \qquad \tau = t
$$

у формулах (15.3) отримуємо

(15.4) 

$$

u(x, t) = \begin{cases} \frac{1}{2} \left[ 1 + \operatorname{erf}\left(\frac{Vt - x}{2\sqrt{Dt}}\right) \right], & Vt > x, \\ \frac{1}{2} \operatorname{erfc}\left(\frac{x - Vt}{2\sqrt{Dt}}\right), & Vt \leq x. \end{cases}

$$

Перед нами стоїть розв'язок задачі конвективної дифузії (15.2). Це дуже легко інтерпретувати, якщо уявити, що ми рухаємося відносно графіка, показаного на рис. 15.6.

![](images/_page_117_Figure_3.jpeg)

Рис. 15.6. Дифузія з області високої концентрації до області низької концентрації. Чим вищий коефіцієнт дифузії, тим швидше буде встановлено стаціонарне значення.

Іншими словами, залежно від відносного значення D (коефіцієнт дифузії) та V (швидкість потоку), розв'язок рухається зліва направо зі швидкістю V, і водночас передній край розширюється зі швидкістю, визначеною значенням D.

![](images/_page_117_Figure_6.jpeg)

Рис. 15.7. Розв'язання задачі конвективної дифузії. Речовина рухається і розсіюється одночасно.

(Рисунок 15.7 показує, як передній фронт концентрації розмивається.)

#### ПРИМІТКИ

Перетворення координат є важливим методом розв'язання рівнянь у похідних похідних. Вибравши правильну систему координат, можна значно спростити рівняння.

#### ЗАВДАННЯ

1. Розв'яжіть задачу Коші:

$$

\begin{array}{ll} u_t = u_{xx} - 2u_x, & -\infty < x < \infty, & 0 < t < \infty, \\ u(x, 0) = \sin x, & -\infty < x < \infty. \end{array}

$$

2. Знайти розв'язок задачі Коші для рівняння конвективної дифузії:

$$

\begin{array}{ll} u_t = u_{xx} - 2u_x, & -\infty < x < \infty, \ 0 < t < \infty, \\ u(x, \ 0) = e^x \sin x, & -\infty < x < \infty. \end{array}

$$

Використайте трансформацію з лекції 8.

3. Знайти рішення задачі переносу:

$$

\begin{array}{ll} (\text{УЧП}) & u_t = -2u_x, \\ (\text{НУ}) & u_t(x, 0) = e^{-x^2}. \end{array} \\ -\infty < x < \infty, \quad 0 < t < \infty,

$$

Перевір свою відповідь.

4. Розв'яжіть задачу

$$

u_t = Du_{xx} - Vu_x, \quad -\infty < x < \infty, \quad 0 < t < \infty,

$$

$$

u(x, 0) = e^{-x^2}, \quad -\infty < x < \infty.

$$

Щё змініті свое свое шення. Як ви зараз приймаєте рішення?

ПРИМІТКА. Перехід до рухомої системи відліку дозволяє одразу відкинути цей термін$ Vu_x $у рівнянні конвективної дифузії. Після розв'язання нової задачі

$$

\begin{array}{ll} (\text{HV}) & u_{\tau} = Du_{\xi\xi}, & -\infty < \xi < \infty, & 0 < \tau < \infty, \\ (\text{HV}) & u(\xi, 0) = e^{-\xi^2}, & -\infty < \xi < \infty, \end{array}

$$

Замінити змінні$ \xi = x - Vt $ ,  $ \tau = t $. У нашому випадку інтеграл можна обчислити явно

$$

u(\xi, \tau) = \frac{1}{2\sqrt{D\pi\tau}} \int_{-\infty}^{+\infty} e^{-\beta^2} e^{-(\xi-\beta)^2/4D\tau} d\beta.

$$

Такий інтеграл уже зустрічався у лекції 12. Можливо, читачу буде зручніше звернутися до таблиць перетворення Фур'є.

# Лекція 16. ОДНОЧАСНЕ ХВІЛОВЕ РІВННЯНЯ (ГІПЕРБОІЧНІ РІВННЯНЯ)

МЕТА ЛЕКЦІЇ: Показати, що осциляції сгруни описуються рівнянням

$$

u_{tt} = \alpha^2 u_{xx}

$$

і що це рівняння є наслідком законів Ньютона. Також розглядаються інші типи хвильового рівняння:

$$

\begin{split} u_{tt} &= \alpha^2 u_{xx} + F\left(x, \ t\right), \\ u_{tt} &= \alpha^2 u_{xx} - \beta u_t - \lambda u + F\left(x, \ t\right). \end{split}

$$

До цього часу ми розглядали фізичні процеси, які описуються одномірними параболічними рівняннями (задачі дифузії). Тепер ми почнемо вивчати інший клас диференціальних рівнянь у похідних — гіперболічні рівняння. Почнемо з одновимірного хвильового рівняння, яке описує, зокрема, поперечні коливання струни.

## Вібрації струн

Розглянемо невеликі коливання струни з нерухомими кінцями. Припустимо, що струна щільно натягнута, зроблена з однорідного матеріалу і коливається в одній площині (рис. 16.1).

![](images/_page_119_Figure_10.jpeg)

Рис. 16.1. Поперечні коливання струни: a — зміщення струни з положення рівноваги.

Дією сили тяжіння на струну знехтуємо. Щоб побудувати математичну модель коливань струни, розглянемо всі сили, що діють на малу ділянку струни (рис. 16.2). Хвильове рівняння є наслідком закону Ньютона: зміна імпульсу дорівнює сумі діючих сил. Нас цікавлять компоненти сил у напрямку, перпендикулярному до осі $x$.

![](images/_page_120_Figure_2.jpeg)

Рис. 16.2. Мала ділянка $(x, x+\Delta x)$ коливної струни.

1. Сила, що виникає через натяг струни ($ \alpha^2 u_{xx} $). Значення поперечної компоненти сили натягу визначається відповідно до формули

Сила рівна

$$
T\sin\theta_2 - T\sin\theta_1
\simeq T\,[u_x(x+\Delta x,t)-u_x(x,t)].
$$

2. Зовнішня сила$ F(x,t) $.

Зовнішня сила F(x, t), прикладена до струни, може довольно залежати від x і t. Нижче наведено деякі приклади зовнішніх сил:

a) гравітаційна сила F(x, t) = -mg;

b) імпульси, розподілені вздовж струни та діючи на неї в різний час:

c) сили, що виникають внаслідок дії звукової хвилі на шкіру барабана (пізніше ми детально вивчаємо коливання мембрани, які описуються хвильовим рівнянням).

3. Сили тертя, що діють на струну (—$ \beta u_t $). Якщо струна вібрує у середньому середовищі, виникає сила опору, яка пропорційна швидкості$ u_t $ .

4. Сила відходу (-úi).

Ця сила спрямована протилежно зміщенню струни. Якщо зміщення u додатне, то сила від'ємна.

Якщо тепер застосувати рівняння руху Ньютона до невеликої ділянки струни, отримаємо

$$

\Delta x \rho u_{tt} = T \left[ u_x \left( x + \Delta x, \ t \right) - u_x \left( x, \ t \right) \right] + \Delta x F \left( x, \ t \right) - \Delta x \beta u_t \left( x, \ t \right) - \Delta x \gamma u \left( x, \ t \right)

$$

где  $ \rho $— лінійна густина струни. Поділимо обидві частини цих відносин на$ \Delta x $І давайте прагнути$ \Delta x $— 0. Результатом є добре відоме телеграфне рівняння

(16.1) 

$$

u_{tt} = \alpha^{2} u_{xx} - \beta u_{t} - \gamma u + F(x, t).

$$

У цьому рівнянні необхідно ділити$ \alpha $ ,  $ \beta $ ,  $ \gamma $та F(x,t) за допомогою$ \rho $, але для простоти ми залишимо попередні позначення для цих величин. Отримано бажане рівняння. Ось інтерпретація найпростішого хвильового рівняння

$$

(16.2) u_{tt} = \alpha^2 u_{xx},

$$

На основі інтуїтивних міркувань.

### Інтуїтивне тлумачення хвильового рівняння

У читача може виникнути питання: чому рівняння виду (16.2) описує коливання струни? Щоб це зрозуміти, треба врахувати фізичний зміст похідних.

![](images/_page_121_Picture_9.jpeg)

Рис. 16.3. Інтерпретація хвильового рівняння $u_{tt}=\alpha^2 u_{xx}$.

Величина $u_{tt}$ є вертикальним прискоренням струни в точці $x$. Отже, рівняння (16.2) можна інтерпретувати так: прискорення струни, зумовлене натягом, у кожній точці тим більше, чим більша кривина $u_{xx}$ у цій точці; коефіцієнт пропорційності дорівнює $\alpha^2=T/\rho$.

#### ПРИМІТКИ

1. Хвильове рівняння$ u_{tt} = \alpha^2 u_{xx} $Вона також описує поздовжні та крутильні коливання стрижня. При поздовжніх коливаннях зміщення паралельні елементу і через$ u\left(x,\,t\right) $Значення поздовжнього зміщення відносно положення рівноваги вказується. Такі коливання виникають, наприклад, коли молоток б'ють по кінці стрижня. Торсіональний

Флуктуації

$$

u_{tt} = ku_{xx}

$$

Тут *k* — це модуль Юнга, який характеризує еластичність матеріалу. Матеріали з великим модулем Юнга коливаються на вищих частотах. Звукові хвилі — це поздовжні хвилі.

2. Якщо лінійна густина струни$ \rho(x) $Залежно від координати, хвильове доповнення записується як

$$

u_{tt} = \frac{\partial}{\partial x} [\alpha^2(x) u_x],

$$

тобто отримується ППП із змінними коефіцієнтами.

3. Оскільки хвильове рівняння$ u_{tt} = \alpha^2 u_{xx} $містить похідну другого порядку часу, щоб отримати єдиний розв'язок у точці t>0, потрібно вказати$ \partial sa $Початкові умови:

$$

u(x, 0) = f(x)

$$

(початкове зміщення струни),$ u_t(x, 0) = g(x) $(початкова швидкість струни).

Таким чином, хвильове рівняння відрізняється від рівняння теплопровідності, для якого потрібно встановити лише одну початкову умову.

4. За допомогою хвильового рівняння можна описати розподіл електричного струму в дроті. З законів Кірхгофа отримуємо таку систему двох рівнянь у похідних похідних першого порядку:

(16.3) 

$$

i_x + Cv_t + Gv = 0, \\ v_x + Li_t + Ri = 0,

$$

где

ж — координата вздовж дроту,

*t* — час,

i(x, t) — розподіл струму вздовж дроту,

 $ v\left(x,\ t\right) $– розподіл потенціалу вздовж дроту,

C — це пропускна здатність дроту на одиницю довжини,

G — це витік на одиницю довжини,

P — опір на одиницю довжини,

-- індуктивність дроту на одиницю довжини.

Рівняння (16.3) зазвичай називають системою телеграфних рівнянь. У такій формі вони зазвичай не використовуються. Щоб перетворити цю систему, диференціюйте перше рівняння на x, друге на t, помножте обидві частини на C і відніміть від першого. У результаті ми отримуємо

$$

i_{xx} + Gv_x - CLi_{tt} - CRi_t = 0.

$$

Тепер використаємо друге рівняння (16.3)

$$

v_{x} = -Li_{t} - Ri,

$$

Нарешті ми отримуємо

(16.4) 

$$

i_{xx} = CLi_{tt} + (CR + GL)i_t + GRi.

$$

Це рівняння для струму, відоме як телеграфне рівняння, є гіперболічним рівнянням другого порядку (якщо, звісно, C і L не дорівнюють нулю, інакше рівняння стає параболічним).

Напруга описується тим самим рівнянням:

(16.5) 

$$

v_{xx} = CLv_{tt} + (CR + GL)v_t + GRv.

$$

Якщо G = R = 0, то рівняння (16.4)—(16.5) спрощуються:

(16.6) 

$$
\begin{aligned}
v_{tt} &= \alpha^2 v_{xx},\\
i_{tt} &= \alpha^2 i_{xx},\qquad \alpha^2=\frac{1}{CL}.
\end{aligned}

$$

#### ЗАВДАННЯ

- 1. Отримайте рівняння (16.5) для *and* з системи з двох рівнянь першого порядку (16.3)
- 2. Знаючи фізичне значення кожного члена хвильового рівняння, що ви можете сказати про поведінку в момент розв'язання наступної задачі:

(УЧП)

$$
u_{tt}=u_{xx}-u_t,\qquad 0<x<1,\quad 0<t<\infty.
$$

(ГУ) 

$$
\begin{cases}
u(0,t)=0,\\
u(1,t)=0,
\end{cases}
\qquad 0<t<\infty.

$$

(НУ) 

$$
\begin{cases}
u(x,0)=\sin(\pi x),\\
u_t(x,0)=0,
\end{cases}
\qquad 0\le x\le 1.

$$

Як виглядає розв'язок наступної задачі в різні моменти часу

$$
\begin{aligned}
(\text{UBP})\quad &u_{tt}=u_{xx}, &&0<x<1,\\
(\text{ГУ})\quad &
\begin{cases}
u(0,t)=0,\\
u(1,t)=\sin t,
\end{cases}
&&0<t<\infty,\\
(\text{НУ})\quad &
\begin{cases}
u(x,0)=0,\\
u_t(x,0)=0,
\end{cases}
&&0\le x\le 1.
\end{aligned}

$$

Дайте фізичне тлумачення цієї задачі.

4. Для рівняння$ u_{tt} = u_{xx} $Знайдіть усі розв'язки такого типу

$$

u(x, t) = e^{ax+bt}

$$

Яке буде рішення двох таких рішень?

# Лекція 17. ФОРМУЛА Д'АЛАМБЕРА

МЕТА ЛЕКЦІЇ: Побудувати розв'язок задачі Коші для хвильового рівняння

$$

(\text{UBP}) \qquad u_{tt} = c^2 u_{xx}, \quad -\infty < x < \infty, \quad 0 < t < \infty,

$$

$$

(\text{HY}) \quad \begin{cases} u(x, 0) = f(x), \\ u_t(x, 0) = g(x), \end{cases} \quad -\infty < x < \infty.

$$

Ця задача описує рух нескінченної струни за заданих початкових умов. Його розв'язав у 1750 році французький математик д'Аламбер. Формула Д'Аламбера

$$

u(x, t) = \frac{1}{2} [f(x - ct) + f(x + ct)] + \frac{1}{2c} \int_{x-ct}^{x+ct} g(\xi) d\xi

$$

полегшує пошук розв'язку, якщо початкові умови відомі. Крім того, це дозволяє дати цікаву фізичну інтерпретацію розв'язку мовою хвиль, що рухаються.

Як читач, ймовірно, пам'ятає, у параболічному випадку ми спочатку розв'язували задачі дифузії на скінченному сегменті (методом розділення змінних), а лише потім переходили до необмежених інтервалів (—$ \infty < x < \infty $), де використовувалося перетворення Фур'є. У гіперболічному випадку ми зробимо навпаки. Почнемо з розв'язання одномірного хвильового рівняння на всій дійсній осі, тобто з задачі Коші

(17.1) 

$$

u_{tt} = c^{2}u_{xx}, \qquad -\infty < x < \infty, \quad 0 < t < \infty,

$$

$$

(17.1) \quad (HY) \quad \begin{cases} u(x, 0) = f(x), \\ u_{t}(x, 0) = g(x), \end{cases} \quad -\infty < x < \infty,

$$

містить лише початкові умови. Цю задачу можна розв'язати або перетворенням Фур'є (для змінної t), або перетворенням Лапласа (для змінної t). Однак ми використаємо інший метод (метод канонічних координат), який, сподіваємося, зацікавить читача. Цей метод базується на тих самих ідеях, що й метод переходу до рухомої системи відліку, з яким ми познайомилися на лекції 15. Тож почнемо розв'язувати задачу (17.1).

#### Розв'язок одновимірного хвильового рівняння. Формула Д'Аламбера

Розв'язок задачі (17.1) буде поділений на кілька етапів:

КРОК 1 (Замінити (x, t) координати на нові канонічні координати$ (\xi, \eta) $ ).

Щоб розв'язати задачу (17.1), ми використаємо той факт, що якщо замінити дві незалежні змінні x і t на нові координати простору-часу

$$

\xi = x + ct,

$$

$$

\eta = x - ct,

$$

Тоді рівняння

$$

u_{ti} = c^2 u_{xx}

$$

перетворюється на рівняння

$$

u_{\xi\eta} = 0

$$

.

Це легко перевірити, якщо ви використовуєте формули перетворення координат:

(17.2) 

$$

u_{x} = u_{\xi} + u_{\eta}, u_{t} = c (u_{\xi} - u_{\eta}), u_{xx} = u_{\xi\xi} + 2u_{\xi\eta} + u_{\eta\eta}, u_{tt} = c^{2} (u_{\xi\xi} - 2u_{\xi\eta} + u_{\eta\eta}).

$$

Підставляючи ці вирази для похідних у хвильове рівняння, отримуємо

$$

u_{\xi\eta} = 0

$$

.. Перший крок зроблено.

КРОК 2 (Розв'язання трансформованого рівняння).

Перетворене рівняння легко розв'язати двома послідовними інтегруваннями (спочатку змінною ξ, а потім η). Після інтегрування на ξ отримуємо

$$

u_{\eta}(\xi, \eta) = \varphi_{1}(\eta)

$$

є довільною функцією від$ \eta $ .

Інтегрування останнього співвідношення за **η** призводить до спільного розв'язку

$$

u(\xi, \eta) = \varphi(\eta) + \psi(\xi),

$$

где  $ \phi(\eta) $є примітивною функцією$ \phi_1(\eta) $ , а  $ \psi(\xi) $– добровільна функція. Отже, загальний розв'язок рівняння

$$

u_{\xi\eta} = 0

$$

записується як

(17.3) 

$$

u(\xi, \eta) = \varphi(\eta) + \psi(\xi),

$$

где  $ \phi(\eta) $  и  $ \psi(\xi) $– довільні функції їхніх аргументів. Наприклад, читач може перевірити, що

$$
u(\xi,\eta)=\sin\eta+\xi^2,\qquad
u(\xi,\eta)=\frac{1}{\eta}+\tan\xi,\qquad
u(\xi,\eta)=\eta^2+e^\xi
$$

є розв'язками рівняння$ u_{\mathrm{ }nh}=0 $. Другий крок зроблено.

КРОК 3 (Повернутися до старих координат$ x $ и $ t $).

Щоб знайти загальний розв'язок, тобто всі розв'язки рівняння$ u_{tt} = c^2 u_{xx} $, заміна

$$

\xi = x - ct,

$$

$$

n = x + ct

$$

до розв'язку

$$

u(\xi, \eta) = \varphi(\eta) + \psi(\xi).

$$

У результаті ми отримуємо

(17.4) 

$$

u(x, t) = \varphi(x-ct) + \psi(x+ct).

$$

Це загальний розв'язок хвильового рівняння. З фізичної точки зору це цікаво, оскільки представляє суму двох хвиль, що рухаються, P

![](images/_page_126_Figure_14.jpeg)

Є. 17.1. Хвиля$ u(x, t) = e^{-(x-ct)^2} $переміщення зліва направо

Наприклад, функції

$$

u(x, t) = \sin(x-ct)

$$

(хвиля, що лежить зліва направо),

$$

u(x, t) = (x + ct)^2

$$

(хвиля, що рухається справа наліво),

$$

u(x, t) = \sin(x - ct) + (x + ct)^2

$$

(дві хвилі, що рухаються в протилежних напрямках)

є типовими розв'язками рівняння волі. На рисунку 17.1 показано найпростішу рухому хвилю.

КРОК 4 (Враховуючи початкові умови).

Нагадаємо читачеві про загальний метод розв'язання задачі Коші в теорії звичайних диференціальних рівнянь. Спочатку знаходять загальне розв'язання рівняння, а потім його підставляють у початкових умовах, щоб знайти конкретні значення довільних констант. У нас схожа ситуація. Щоб розв'язати задачу Коші для хвильового рівняння, підставляємо загальне розв'язання

$$

u(x, t) = \varphi(x - ct) + \psi(x + ct)

$$

(містить дві довільні функції) у початкових умовах

$$

u(x, 0) = f(x),

$$

 $ u_t(x, 0) = g(x), $Щоб знайти конкретні вирази для довільних функцій f і f.

(17.5) 

$$

\varphi(x) + \psi(x) = f(x), \\ -c\varphi'(x) + c\psi'(x) = g(x).

$$

Якщо тепер інтегрувати друге рівняння (17.5), отримаємо алгебранічну систему з двох рівнянь за невідомими функціями$ \varphi(x) $  и  $ \psi(x) $. Насправді, після інтеграції в$ x_0 $До x ми отримуємо

(17.6) 

$$

-c\varphi(x) + c\psi(x) = \int_{x_0}^{x} g(\xi) d\xi + K.

$$

Тепер розв'яжіть рівняння (17.6) разом із першим рівнянням (17.5) і отримайте наступні вирази для функцій$ \varphi(x) $  и  $ \psi(x) $ :

(17.7) 

$$

\psi(x) = \frac{1}{2} f(x) + \frac{1}{2c} \int_{x_0}^{x} g(\xi) d\xi + K/2c,

$$

$$

\varphi(x) = \frac{1}{2} f(x) - \frac{1}{2c} \int_{x_0}^{x} g(\xi) d\xi - K/2c.

$$

Отже, загальне розв'язання задачі (17.1) задається формулою

(17.8) 

$$

u(x, t) = \frac{1}{2} \left[ f(x-ct) + f(x+ct) \right] + \frac{1}{2c} \int_{x-ct}^{x+ct} \mathbf{g}(\xi) d\xi.

$$

Мета досягнута. Ми отримали загальне розв'язання задачі (17.1). Розв'язок у вигляді (17.8) зазвичай називають формулою д'Аламбера. Читача запрошують самостійно слідкувати за розташуванням меж інтегрування, щоб переконатися, що вони змінюються з x-ct на x+ct. Задача повністю вирішена.

Перед завершенням лекції ми наведемо кілька прикладів, які показують, як застосовувати формулу д'Аламбера до конкретних валахів.

## Приклади застосувань формули д'Аламбера

## 1. Рух синузондалних хвиль

Розглянемо початкові умови форми

$$
u(x,0)=\sin x,\qquad u_t(x,0)=0.
$$

Використовуючи формулу д'Аламбера, отримуємо розв'язок

$$

u(x, t) = \frac{1}{2} [\sin(x-ct) + \sin(x+ct)].

$$

Її можна інтерпретувати так. Початкове зміщення струн$ u(x, 0) = \sin x $поділяється на дві однакові частини

$$

\frac{\sin x}{2}

$$

 H  $ \frac{\sin x}{2} $ .

Кожна з частин поширюється зі швидкістю c у вигляді хвиль, що рухаються. Одна з хвиль рухається зліва направо, а інша — у протилежному напрямку. Читача запрошують замислитися, як виглядає отримана хвиля у цьому випадку.

# 2. Поширення найпростішого квадратного кутового моменту

У цьому випадку початкові умови встановлюються так:

$$

u(x, 0) = \begin{cases} 1, & -1 < x < 1, \\ 0, & \end{cases}

$$

 $ u_t(x, 0) = 0 $(в інших моментах).

Оскільки початкове зміщення розділяється на дві півхвилі, що рухаються в протилежних напрямках, отриманий рух буде таким, як показано на рис. 17.2.

![](images/_page_129_Figure_2.jpeg)

Рис. 17.2. Початкове збурення розділяється на дві хвилі, що рухаються.

#### 3. Початкова швидкість вказана

Тепер припустимо, що в початковий момент струна перебуває в рівноваговій позиції. Давайте дамо струні початкову швидкість (як у фортепіано) форми$ \sin x $, тобто

$$

u(x, 0) = 0,

$$

$$

u_t(x, 0) = \sin x.

$$

Ми знаходимо розв'язок за формулою д'Аламбера

$$

u(x, t) = \frac{1}{2c} \int_{x-ct}^{x+ct} \sin \xi \, d\xi = \frac{1}{2c} \left[ \cos (x+ct) - \cos (x-ct) \right].

$$

Вона представляє суму двох бігових косинусних заповітів. Читачеві слід запитати себе, чи здається йому це рішення розумним.

На цьому завершується лекція 17. У наступній лекції ми покажемо, як формула д'Аламбера допомагає дати інтерпретацію у площині змінних x і t.

#### ПРИМІТКИ

- 1. Загальний розв'язок рівняння з частковими похідними похідними другого порядку містить дві довільні функції, а загальний розв'язок звичайного диференціального рівняння містить дві довільні константи. Це означає, що диференціальне рівняння в похідних похідних має більше розв'язків, ніж звичайне диференціальне рівняння.
- 2 Заміна змінних для зведення диференціального рівняння в похідних до простішої форми є одним із загальних методів теорії. Нові координати (ξ, η) називаються канонічними координатами. Ми будемо звертатися до них у майбутньому, особливо у вивченні гіперболічних рівнянь.
- 3 Метод пошуку загального розв'язку диференціального рівняння в похідних похідних і заміни цього розв'язку на початкові умови не є загальним методом розв'язання рівнянь у похідних похідних. Розв'язок, який обговорюється в цій лекції, є єдиним прикладом використання цього методу. Зазвичай неможливо знайти загальне розв'язання диференціального рівняння з похідними похідними, але навіть якщо це можливо, дуже складно підставити цей розв'язок граничними умовами.

#### ЗАВДАННЯ

1. Перевірити, чи розв'язок д'Аламбера (17-8) задовольняє початкові умови задачі (17.1).

2. Замінити (17.7) у загальному розв'язку$ u(x, t) = \psi(x - ct) + \psi(x + ct) $і отримати формулу д'Аламбера.

3. Знайти рішення задачі Коші:

$$

(\text{UBP}) \qquad u_{tt} = u_{xx}, \quad -\infty < x < \infty, \quad 0 < t < \infty, \\ (\text{HV}) \qquad \left\{ \begin{array}{ll} u(x, \ 0) = e^{-x^2} \\ u_t(x, \ 0) = 0. \end{array} \right. \quad -\infty < x < \infty, \\ \end{array}

$$

Який вигляд має цей розв'язок у різні моменти часу? 4. Знайдіть розв'язок задачі Коші:

(УЧП) 

$$

u_{tt} = u_{xx}, -\infty < x < \infty, 0 < t < \infty,

$$

(НУ) 

$$

\begin{cases} u(x, 0) = 0, \\ u_t(x, 0) = xe^{-\lambda^2}, -\infty < x < \infty, \end{cases}

$$

Побудуйте хронологію рішень у різні моменти часу. 5. Розв'язання рівнянь (17.6) і першого рівняння (17.5) разом,

Візьміть формули (17.7).

# Лекція 18. ФОРМУЛА Д'АЛАМБЕРА (ПРОДОВЖЕННЯ)

МЕТА-ЛЕКЦІЯ: Щоб показати, як за допомогою формули Д'Аламбера можна знайти зв'язок між рівнем Хвіла і прямою лінією.

(УЧП)

$$
u_{tt}=c^2u_{xx}, \qquad 0<x<\infty,\quad 0<t<\infty.
$$

(ГУ)

$$
u(0,t)=0,\qquad 0<t<\infty.
$$

(НУ)

$$
u(x,0)=f(x),\qquad u_t(x,0)=g(x),\qquad 0\le x<\infty.
$$

Інтерпретація формули Д'Аламбера в координатній області подається$ x, t $.

У попередній лекції показано, як змінюється початкова частина струни$ u(x,0)=f(x) $І Початкова Швидкіст$ u_t(x,0)=g(x) $, то

(18.1)

$$
u(x,t)=\frac{1}{2}\big[f(x-ct)+f(x+ct)\big]+\frac{1}{2c}\int_{x-ct}^{x+ct} g(\xi)\,d\xi
$$

Опишіть зміну струни в цей момент.

У цій лекції ми можемо побачити інтерпретацію формули в області$ x $ і $ t $(простора зона годинника) показана як модифікація для завдання розрізання надрукованої струни.

# Прозоро-часова інтерпретація формули Д'Аламбера

Див. завдання Коші:

(18.2)

$$
u_{tt} = c^2 u_{xx}, \quad -\infty < x < \infty,\quad t > 0
$$

Розвъязок зазажаєть на плішчіні$ (x,t) $, не в координатах$ (x,u) $.

![](images/_page_133_Figure_5.jpeg)

Фіг. 18.2. Розподіл завдань Коші в регіоні$ x,t $:  
a — передній фасад хвилі, ще рухайця вліво;
b — задня передня частина hvyli;
C — передня частина hvyli, яка знаходиться праворуч.

Тепер можна побачити втрату, якщо зміна cob дорівнює нулю, а cob повний.

# Вipadок 2 (почакове зміщення дорівняе nullю)

Rozglyanemo pocobkovi umovi:

$$
u(x,0)=0,\quad u_t(x,0)=g(x).
$$

Роз'язок — це вігляда формули Д'Аламбера.

Значення розвитку на цій точці$ (x_e,t_e) $Це можна інтерпретувати як інтеграцію початкової частини мозку$ [x-ct, x+ct] $.

![](images/_page_134_Figure_2.jpeg)

Фіг. 18.3. Інтеграція завдань за допомогою COB.

Перші підходи до методу Рітси можуть включати:

$$
u_1(x,y)=a_1xy(1-x)(1-y),
$$

$$
u_3(x,y)=xy(1-x)(1-y)(a_1+a_2x+a_3y),
$$

І поки що.

Військова функціональність:

$$
J[u_n] = \int_0^1 \int_0^1 \left[\left(\sum_{j=1}^n a_j \frac{\partial \varphi_j}{\partial x}\right)^2 + \left(\sum_{j=1}^n a_j \frac{\partial \varphi_j}{\partial y}\right)^2 + 2f \sum_{j=1}^n a_j \varphi_j\right] dx\,dy.
$$

Розум мінімуму:

$$
\frac{\partial J[u_n]}{\partial a_k}=0,\quad k=1,\dots,n.
$$

Завдання полягає у зведенні до системи лінійних алгебраїчних рівнів для коефіцієнтів$ a_1,\dots,a_n $.

Розробивши систему, ми можемо побачити наближення задач розвитку.

# ПОВАГА

1. Ця лекція за методом зміни Рітки має дві різні функції. Аналогічно, можна мінімізувати функції інших типів, застосовуючи:

$$
y(0)=0,\quad y(1)=1.
$$

2. Для Хвилового Ривнянного

$$
u_{tt}=c^2u_{xx}, \quad 0<x<\infty,\quad t>0,
$$

З пограничним і «початковим розумом»:

$$
u(0,t)=0,\quad u(x,0)=f(x),\quad u_t(x,0)=g(x),
$$

буде розв'язана за допомогою формули Д'Аламбера.
# Розв'язання задачі для напівнескінченного стронги за допомою формула Д'Аламберта

Наша мета — знайти хвілові рухи співаних струн, лева тисяч закриттів, для завдань розумів кобів. Для розв'язання задач (18.6) ми використовуємо той самий метод, який використовується для досягнення потрібного рівня в довгостроковій розробці.

Zahalnyi rozvyazok khvylovy rivnyannya maje viglyad

$$
u(x,t)=\varphi(x-ct)+\psi(x+ct).
$$

Подано в початку думок і вранці (як у передлекції):

(18.7)

$$
\varphi(x)+\psi(x)=f(x),\qquad
-c\varphi'(x)+c\psi'(x)=g(x).
$$

Тепер ми стоїмо перед завданням, якого недостатньо для ненатягнутої нитки. Розв'язок$ u(x,t) $винний у першому квадранті$ (x>0,\ t>0) $Райони змінних земель$ x $ і $ t $. Звідти необхідно обчислити функції

$$
\varphi(x-ct), \quad \psi(x+ct)
$$

для всіх$ -\infty < x-ct < \infty $ та $ 0 < x+ct < \infty $.

Шкода, перша з формул (18.7) дозволяє нам розраховувати$ \varphi(x-ct) $Lysh in$ x-ct \ge 0 $, oskilky pochatkovi funktsii$ f(x) $ і $ g(x) $Вказано тілкі для значення аргументу.

Якщо $ x - ct \ge 0 $, тоді буде розглянуто написання постпозиції (18.7) у західному розвитку наступного:

Для співочої струни з'єднань записуйте від руки:

$$
u(x,t) = \varphi(x-ct) + \psi(x+ct)
= \frac{1}{2}\bigl[f(x-ct) + f(x+ct)\bigr] + \frac{1}{2c} \int_{x-ct}^{x+ct} g(\xi)\,d\xi.
$$

Якщо $ x \ge ct $, ця формула базується на специфічній формулі Д'Аламбера для невизначеного рядка. Якчо ж$ x < ct $, необхідно vrahovaty borderline umov$ u(0,t)=0 $, тобто відбиття хвили від закрістеренного кінця.

Результат для співаючої струни можливий

$$
u(x,t)=
\begin{cases}
\dfrac12\bigl[f(x-ct)+f(x+ct)\bigr]+\dfrac{1}{2c}\displaystyle\int_{x-ct}^{x+ct} g(\xi)\,d\xi, & x \ge ct, \\
\dfrac12\bigl[f(x+ct)-f(ct-x)\bigr]+\dfrac{1}{2c}\displaystyle\int_{ct-x}^{x+ct} g(\xi)\,d\xi, & x < ct.
\end{cases}
$$

Цим завершиться зародженням роз'язку. Геометрична інтерпретація наведена на рис. 18.6.

![](images/_page_137_Figure_9.jpeg)

Фіг. 18.6. Інтерпретація задачі Коші для виробництва струн у$ xt $-Площа:$ a $— збурення відбиваєть від межі;$ \delta $– Розширити характеристики точки$ (x_0+ct_0,\,0) $.
# ПОВАГА
1. Формула для написання рядка права на звуження з фізичною картинкою:$ x \ge ct $Хвиля ще не усвідомив вхід між межами, і на момент$ x<ct $У розв'язку з'выяєть відбита хвля зі зіненім znak.

2. Якчо замініті граничну умову$ u(0,t)=0 $на іншу, то явный вигляд розв'язку теж зінітьсь. Ale sama ideya pobudovi — позначення складів хвіловів за допомогою прикордонного розуму — позбавлене того ж значення.

3. Прямі

$$
x-ct=\text{const}, \qquad x+ct=\text{const},
$$

називаються характеристиками Хвілової ривнянки. Найпряміші розширюються.

# ЗАВДАННЯ

1. Попросіть графіки записувати рядок у точний момент і годину і поясніть, як закривається межа.

2. Опишіть алгоритм наступної розробки:
- продовжувати дані COB для всього світу,
- побудувати ліву і pravo хвлі,
- Врахуваті відбіття від межі$ x=0 $.

3. Розробити завдання для написання рядка, якщо дані налаштовані графічно.

![](images/_page_139_Figure_3.jpeg)

**4.** Припустимо, що колівня stringи описують рівнянням

$$
u_{tt} = u_{xx},
$$

  
Pochatkové zmishchennya zmýschennya zámýrazhenene na fignku, a pochatkový shvydkist' dorivnuє nil. Сформувати розвиток цих завдань у$ x $–$ t $Площини. Відновити повагу, яка є основою розуму в цій справі, не означає її розвивати.

![](images/_page_139_Figure_8.jpeg)

# Лекція 19. HWIŁOWE RIVNYANNIA I BORDERLINE UMOVI
**Meta lektsii.** Oznayomiti chitacha s osnovnymi tipami granichnykh umov dlya hvilovogo rivnyannya na skinchennomu vidrezku.

Побачити одне світське рівнянню

$$
u_{tt} = c^2 u_{xx}, \qquad 0 < x < L,\quad 0 < t < \infty.
$$

Для цієї мети існує три типи прикордонних розумів:

1. **Змити першу лінію**:

$$
u(0,t)=g_1(t), \qquad u(L,t)=g_2(t).
$$

2. **Змивка іншого виду**:

$$
u_x(0,t)=g_1(t), \qquad u_x(L,t)=g_2(t).
$$

3. **Умов третього виду**:

$$
\alpha_1 u_x(0,t)+\beta_1 u(0,t)=g_1(t), \qquad
\alpha_2 u_x(L,t)+\beta_2 u(L,t)=g_2(t).
$$

У випадку фізичного чуття мета полягає в тому, щоб побачити заданий режим на межі, задану силу згинів струни.

Далеко від мене, майже від розумів першого роду і погляду на завдання

$$
\begin{aligned}
u_{tt} &= c^2u_{xx}, && 0 < x < L,\quad 0 < t < \infty, \\
u(0,t) &= g_1(t), \quad u(L,t) = g_2(t), && 0 < t < \infty, \\
u(x,0) &= f(x), \quad u_t(x,0) = g(x), && 0 \leqslant x \leqslant L.
\end{aligned}
$$

Щоб розробити таку гетерогенну задачу, ручно внести внесок у суму спеціально обраної функції, задовольнити прикордонні уми та створити нові задачі з односторонніми прикордонними розумами. Те саме, що й підхід і буде далі вікорістоно.

# ПОВАГА

1. Прикордонні умові першого роду встановлюють заміну на нитку кінтсах. Для скручених струн ce,$ u(0,t)=0 $ і $ u(L,t)=0 $.

2. Граничні умови другого роду затают похідну$ u_x $На кордоні. У механічному тлумаченні це описує силу струн на родину.

3. Умові третиго роду поедну значення функції та їїй похідної. Це означає перемогу, якщо нитки замкнень замкнені.

4. До того, як метод диференціації різних прикордонних розумів часто ручно перетворюється в єдиний. Щоб цей розв'язок служив на вігляді

$$u(x,t)=w(x,t)+U(x,t),$$

де $ w(x,t) $обирати так, щоб війна була задоволена прикордонними умовами, і для$ U(x,t) $Виконати завдання без жодних меж.

# ЗАВДАННЯ

1. Для завдань

$$
\begin{aligned}
u_{tt} &= c^2u_{xx}, && 0<x<L,\quad 0<t<\infty,\\
u(0,t)&=0,\quad u(L,t)=0, && 0<t<\infty,\\
u(x,0)&=f(x),\quad u_t(x,0)=g(x), && 0\le x\le L,
\end{aligned}
$$

щоб пояснити фізичну форму шкіри розуму.

2. Записати граничні умови для ниток, лева законів як закриття, і правильні люди відстануть від закону$ h(t) $.

3. Показит, як заміною$ u(x,t)=w(x,t)+U(x,t) $perevoditi umovi

$$u(0,t)=g_1(t),\qquad u(L,t)=g_2(t)$$

Водночас для нової функції$ U(x,t)$.
